var documenterSearchIndex = {"docs":
[{"category":"section","location":"generated/bald_classification/#Data-efficiency-with-BinaryBALD:-learning-curves-on-a-checkerboard","page":"Decision boundary (BinaryBALD)","text":"Active learning earns its keep only when each observation is expensive. A drug-discovery ADMET panel runs roughly $3k–$8k per compound; synchrotron beam time costs several hundred dollars per hour; expert image labeling can consume person-years.  When labels are cheap and abundant, a larger passively-collected dataset simply wins — standard ML is the right tool.  But when each query has a real cost, the question is no longer how accurate can we get? but how many queries does it take to reach a target accuracy?\n\nThis vignette makes that question concrete.  We compare two arms on a checkerboard classification task:\n\nBinaryBALD — selects each query to maximally reduce uncertainty about the decision boundary (mutual information between label and model parameters; Houlsby et al. 2011).\nUniform random — draws the next query uniformly at random with no acquisition signal.\n\nThe headline is a learning curve — test accuracy vs number of queries, averaged over 10 paired seeds with ±1 SD bands.","title":"Data efficiency with BinaryBALD: learning curves on a checkerboard"},{"category":"section","location":"generated/bald_classification/#Setup","page":"Decision boundary (BinaryBALD)","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, LinearAlgebra, Random\nusing Statistics: mean, std\nusing Plots; gr()","title":"Setup"},{"category":"section","location":"generated/bald_classification/#The-task:-a-checkerboard-decision-boundary","page":"Decision boundary (BinaryBALD)","text":"We classify points in [−2, 2]² by the sign of sin(2x₁)·sin(2x₂). This produces an interleaved checkerboard pattern with four sign-alternating cells in each quadrant — genuinely harder than a convex boundary because the learner must resolve multiple disconnected regions simultaneously.  That multi-region structure is exactly what rewards informative sampling most.\n\nlabel(x) = sin(2x[1]) * sin(2x[2]) > 0\n\nbox = Box([-2.0, -2.0], [2.0, 2.0])","title":"The task: a checkerboard decision boundary"},{"category":"section","location":"generated/bald_classification/#Learning-curves:-methodology","page":"Decision boundary (BinaryBALD)","text":"A held-out test set of 400 random points is fixed once (seed 0) and reused across every run.  Test accuracy is the idiomatic AL metric: it measures generalisation to unseen points rather than memorisation of training data.\n\nFor each of 10 seeds we run both arms from the same cold start (10 random points drawn with that seed), then step each arm one query at a time up to budget 40.  Sharing the cold start ensures a fair paired comparison. Averaging over 10 seeds gives stable mean curves and honest ±1 SD bands — the reproducibility literature shows single-run AL claims are often noise.\n\n# Fixed held-out test set, seeded once\nRandom.seed!(0)\nXtest = [4 .* rand(2) .- 2 for _ in 1:400]\nytest = label.(Xtest)\nacc(g) = sum((predmean(g, p) > 0) == yt for (p, yt) in zip(Xtest, ytest)) / length(Xtest)\n\nfunction learning_curve(seed; bald::Bool, B = 40, ℓ = 0.4)\n    Random.seed!(seed)\n    cold = [4 .* rand(2) .- 2 for _ in 1:10]    # paired: same seed ⇒ identical cold start for both arms\n    al = ActiveLearner(LaplaceGP(with_lengthscale(SqExponentialKernel(), ℓ)), BinaryBALD())\n    for x in cold\n        observe!(al, x, label(x))\n    end\n    accs = Float64[]\n    for _ in 1:B\n        x = bald ? acquire(al; over = box) : (4 .* rand(2) .- 2)   # BALD-selected vs uniform-random\n        observe!(al, x, label(x))\n        push!(accs, acc(posterior_gp(al)))\n    end\n    return accs\nend\n\nSEEDS = 1:10\nbald_curves = [learning_curve(s; bald = true)  for s in SEEDS]\nrand_curves = [learning_curve(s; bald = false) for s in SEEDS]\nmb = [mean(c[k] for c in bald_curves) for k in 1:40]   # mean BALD curve\nmr = [mean(c[k] for c in rand_curves) for k in 1:40]   # mean random curve\nsb = [std(c[k]  for c in bald_curves) for k in 1:40]   # ±1 SD for ribbons\nsr = [std(c[k]  for c in rand_curves) for k in 1:40]","title":"Learning curves: methodology"},{"category":"section","location":"generated/bald_classification/#Figure-1-—-the-headline-learning-curve","page":"Decision boundary (BinaryBALD)","text":"BinaryBALD reaches 80% test accuracy around query 30 on average; uniform random does not cross that threshold within the 40-query budget.  The ±1 SD bands show the variation across seeds: the gap is consistent, not a lucky draw.\n\nWhy?  Uniform random spreads its budget across the whole domain, wasting queries in regions far from any decision boundary.  BinaryBALD's mutual-information criterion spends each query where it most reduces uncertainty about the boundary, compressing the path to the target accuracy.\n\nCaveats (important).  AL assumes a reasonably calibrated model.  At very low budgets the posterior is noisy and BALD can match or trail random; and if observations are genuinely cheap and plentiful, passive sampling wins.  A natural additional baseline — not shown here — is max-entropy / uncertainty sampling (query wherever σ(x) is largest); that is a small acquisition to add and would provide a tighter comparison than uniform random alone.\n\nplt1 = plot(\n    mb;\n    ribbon = sb,\n    label = \"BinaryBALD\",\n    xlabel = \"queries (after cold start)\",\n    ylabel = \"test accuracy\",\n    title = \"Data efficiency: BinaryBALD vs random\",\n    legend = :bottomright,\n    ylims = (0.5, 1.0),\n    linewidth = 2,\n)\nplot!(\n    plt1, mr;\n    ribbon = sr,\n    label = \"uniform random\",\n    linewidth = 2,\n)\nhline!(\n    plt1, [0.8];\n    ls = :dash,\n    lc = :black,\n    lw = 1,\n    label = \"80% target\",\n)","title":"Figure 1 — the headline learning curve"},{"category":"section","location":"generated/bald_classification/#Figure-2-—-snapshot-at-budget-40-(seed-1)","page":"Decision boundary (BinaryBALD)","text":"The latent GP mean predmean(g, p) is the pre-sigmoid score: positive in label = true cells, negative in label = false cells.  With the :RdBu map, blue marks the positive class and red the negative; the true checkerboard boundaries are overlaid in black.  The latent mean has recovered the alternating four-cell structure.  White dots are the 40 BALD queries (after the 10-point cold start): rather than hugging any single contour, BinaryBALD spreads them to reduce uncertainty across the disconnected regions — an information-driven coverage that lets it resolve the whole pattern within budget.\n\n# Reproduce seed-1 BALD run for the snapshot\nRandom.seed!(1)\ncold1 = [4 .* rand(2) .- 2 for _ in 1:10]\nal1 = ActiveLearner(LaplaceGP(with_lengthscale(SqExponentialKernel(), 0.4)), BinaryBALD())\nfor x in cold1\n    observe!(al1, x, label(x))\nend\nfor _ in 1:40\n    x = acquire(al1; over = box)\n    observe!(al1, x, label(x))\nend\ng1 = posterior_gp(al1)\n\nxs = range(-2, 2; length = 50)\nys = range(-2, 2; length = 50)\n\nZ_mean = [predmean(g1, [xi, yj]) for yj in ys, xi in xs]\n\n# True checkerboard boundaries: contour of sin(2x₁)·sin(2x₂) at level 0\nZ_check = [sin(2xi) * sin(2yj) for yj in ys, xi in xs]\n\npts1 = queried_points(al1)\npx1 = [p[1] for p in pts1]\npy1 = [p[2] for p in pts1]\n\nplt2 = heatmap(\n    xs, ys, Z_mean;\n    title = \"Latent posterior mean, seed 1 (40 BALD queries)\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :RdBu,\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\ncontour!(plt2, xs, ys, Z_check; levels = [0.0], lw = 2, lc = :black, label = \"true boundary\")\nscatter!(\n    plt2, px1, py1;\n    ms = 4,\n    mc = :white,\n    msw = 1,\n    label = \"BALD queries\",\n)\n\nThe example's correctness checks run when this file is executed directly (e.g. in CI); they are stripped from this rendered page.\n\n\n\nThis page was generated using Literate.jl.","title":"Figure 2 — snapshot at budget 40 (seed 1)"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"The public surface of Magpie.jl. Names are grouped by role; see the Examples for worked usage.","title":"API Reference"},{"category":"section","location":"api/#Models-and-inference","page":"API Reference","text":"","title":"Models and inference"},{"category":"section","location":"api/#Acquisitions","page":"API Reference","text":"","title":"Acquisitions"},{"category":"section","location":"api/#Maximizing-an-acquisition","page":"API Reference","text":"","title":"Maximizing an acquisition"},{"category":"section","location":"api/#Active-learning-loop","page":"API Reference","text":"","title":"Active-learning loop"},{"category":"type","location":"api/#Magpie.AbstractGPModel","page":"API Reference","text":"AbstractGPModel <: AbstractGPs.AbstractGP\n\nContract for a GP that plugs into Magpie's active-learning loop and SciML bridge.\n\nSubtyping AbstractGPs.AbstractGP inherits the Distributions interface (f(x) → FiniteGP, rand, logpdf, mean/var/cov). On top of that, an AbstractGPModel is expected to implement the extended contract the loop drives:\n\nupdate(g, X, y) — incremental conditioning on new observations\nfit(g; …) — hyperparameter optimization\npredmean(g, u) — posterior mean at a single point\n\nSubtype this and implement those methods to plug in a new GP (e.g. a sparse GP). Bundled implementations: ExactGP, LaplaceGP.\n\n\n\n\n\n","title":"Magpie.AbstractGPModel"},{"category":"type","location":"api/#Magpie.ExactGP","page":"API Reference","text":"ExactGP{Tp,Tx,Tδ,TC,Tα} <: AbstractGPModel\n\nExact-regression GP holding the cached state of an incremental conditioning. The posterior mean reuses the cached weights alpha:\n\nmu(x_ast) = m(x_ast) + mathrmcov(x_ast x)alpha\nqquad alpha = C^-1delta\n\nField names follow Rasmussen & Williams (Algorithm 2.1).\n\nFields\n\nprior: the prior AbstractGPs.GP (mean + kernel)\nx: training inputs\nδ: residuals delta = y - m(x) against the prior mean\nC: Cholesky factorization of K + sigma^2 I, extended in place on each update\nα: cached weights alpha = C^-1delta\nnoise: observation-noise variance sigma^2 added to the kernel diagonal\n\nConstructor\n\nExactGP(kernel::Kernel; noise=1e-6, mean=AbstractGPs.ZeroMean())\n\nBuild an unconditioned ExactGP; condition it on data with update.\n\n\n\n\n\n","title":"Magpie.ExactGP"},{"category":"type","location":"api/#Magpie.LaplaceGP","page":"API Reference","text":"LaplaceGP{Tp,Tx,Ty,Ta,TW,TL} <: AbstractGPModel\n\nBinary-classification GP using the Laplace approximation (Rasmussen & Williams, Algorithm 3.1) with a logistic likelihood. The latent posterior is approximated by a Gaussian centred at the MAP hatf with precision\n\nSigma^-1 = K^-1 + W qquad B = I + sqrtWKsqrtW\n\nwhere W is the (diagonal) likelihood Hessian and B is the well-conditioned matrix actually factorized. The MAP is re-fit from scratch on each update, so the full observation history is retained. Field names follow R&W Algorithm 3.1.\n\nFields\n\nprior: the prior AbstractGPs.GP over the latent function f\nx: training inputs\ny: Bool class labels, kept so re-fitting can use the full history\na: dual vector K^-1(hatf - m) at the MAP latent hatf\nW: diagonal of the likelihood Hessian, W_i = hatpi_i(1 - hatpi_i), at the MAP\nL: Cholesky factor of B = I + sqrtWKsqrtW at the MAP\n\nConstructor\n\nLaplaceGP(kernel::Kernel; mean=AbstractGPs.ZeroMean())\n\nBuild an unconditioned LaplaceGP; condition it on labels with update.\n\n\n\n\n\n","title":"Magpie.LaplaceGP"},{"category":"function","location":"api/#Magpie.update","page":"API Reference","text":"update(g::ExactGP, X, y) -> ExactGP\n\nCondition the GP on observations (X, y), returning a new conditioned ExactGP.\n\nThe first call factorizes K + σ²I from scratch; later calls extend the existing Cholesky factor incrementally via AbstractGPs.update_chol. A scalar y conditions on a single point.\n\n\n\n\n\nupdate(g::LaplaceGP, X, y::AbstractVector{Bool}) -> LaplaceGP\n\nCondition the classifier on new labelled inputs (X, y). Because the Laplace MAP is re-fit from scratch, the new data is appended to the stored history and the MAP solve (_laplace_fit) is rerun over the full set.\n\n\n\n\n\n","title":"Magpie.update"},{"category":"function","location":"api/#Magpie.predict","page":"API Reference","text":"predict(g, xs) -> (mean, var)\n\nPosterior mean and marginal variance at the inputs xs (an alias for mean_and_var on any AbstractGP).\n\n\n\n\n\n","title":"Magpie.predict"},{"category":"function","location":"api/#Magpie.predmean","page":"API Reference","text":"predmean(g::ExactGP, u) -> Real\n\nPosterior mean at a single input u, returned as a scalar (unlike mean(g, [u]), which returns a length-1 vector).\n\n\n\n\n\nPosterior latent mean at a single input u, as a scalar; >0 predicts the positive class.\n\n\n\n\n\n","title":"Magpie.predmean"},{"category":"function","location":"api/#Magpie.nlml","page":"API Reference","text":"nlml(g::ExactGP) -> Real\n\nNegative log marginal likelihood, the objective fit minimizes:\n\n-log p(y mid X) = tfrac12delta^top alpha\n    + sum_i log C_ii + tfracn2log 2pi\n\nwhere delta = y - m(X), alpha = C^-1delta, and C is the upper Cholesky factor of K + sigma^2 I. Returns 0.0 for an unconditioned GP.\n\n\n\n\n\n","title":"Magpie.nlml"},{"category":"function","location":"api/#Magpie.fit","page":"API Reference","text":"fit(g::ExactGP; restarts=1, ad=AutoForwardDiff()) -> ExactGP\n\nOptimize the kernel lengthscale by minimizing nlml with LBFGS, returning a GP re-conditioned at the best lengthscale found.\n\nOptimization runs in log-space (logℓ, unconstrained, bounded to [-6, 6]) so the recovered ℓ = exp(logℓ) stays positive. With restarts > 1, extra runs start from the initial logℓ jittered in log-space and the lowest-NLML result wins. ad selects the DifferentiationInterface backend for the gradient.\n\nnote: Note\nv1 assumes the prior kernel is with_lengthscale(SqExponentialKernel(), ℓ).\n\n\n\n\n\n","title":"Magpie.fit"},{"category":"type","location":"api/#Magpie.AcquisitionFunction","page":"API Reference","text":"AcquisitionFunction\n\nSupertype for active-learning acquisitions. An acquisition is callable as a(g, x), scoring a candidate input x under the current GP g; the loop queries where the score is largest (see acquire).\n\n\n\n\n\n","title":"Magpie.AcquisitionFunction"},{"category":"type","location":"api/#Magpie.MarginalAcquisition","page":"API Reference","text":"MarginalAcquisition <: AcquisitionFunction\n\nAcquisitions that depend only on the GP's marginal posterior at x (its mean and variance), so each candidate can be scored independently.\n\n\n\n\n\n","title":"Magpie.MarginalAcquisition"},{"category":"type","location":"api/#Magpie.Straddle","page":"API Reference","text":"Straddle{T} <: MarginalAcquisition\n\nLevel-set \"straddle\" acquisition (Bryan et al. 2005):\n\na(x) = betasigma(x) - mu(x) - h\n\nIt is large where the posterior is uncertain (sigma high) and close to the target level h, driving queries onto the level set x  f(x) = h.\n\nConstructor\n\nStraddle(; h=0.0, β=1.96)\n\nh is the target level and β the exploration weight (the default ≈ a 95% band).\n\n\n\n\n\n","title":"Magpie.Straddle"},{"category":"type","location":"api/#Magpie.RandStraddle","page":"API Reference","text":"RandStraddle{T,R} <: MarginalAcquisition\n\nRandomized straddle (Inatsu et al. 2024): like Straddle but the band width β is redrawn each round as √(−2 log u), u ~ U(0,1), giving a theoretically grounded exploration schedule. Call resample between rounds to redraw.\n\nConstructor\n\nRandStraddle(; h=0.0, rng=Random.default_rng())\n\nh is the target level; rng seeds the band-width draw. The field sβ stores the current √β.\n\n\n\n\n\n","title":"Magpie.RandStraddle"},{"category":"type","location":"api/#Magpie.BinaryBALD","page":"API Reference","text":"BinaryBALD <: AcquisitionFunction\n\nBayesian Active Learning by Disagreement for binary classification (Houlsby et al. 2011): the mutual information between the label y and the latent function f,\n\nmathbbIy f mid x mathcalD\n    = mathrmHy mid x mathcalD - mathbbE_fmathrmHy mid f\n\ncomputed in bits. Large where the GP is uncertain about the class probability, driving queries that are most informative about the decision boundary. Pair with a LaplaceGP.\n\n\n\n\n\n","title":"Magpie.BinaryBALD"},{"category":"function","location":"api/#Magpie.resample","page":"API Reference","text":"resample(a::AcquisitionFunction) -> AcquisitionFunction\n\nReturn a copy of a with any per-round randomness redrawn. Stochastic acquisitions like RandStraddle draw a fresh band width; deterministic ones return a.\n\n\n\n\n\n","title":"Magpie.resample"},{"category":"type","location":"api/#Magpie.AcquisitionDomain","page":"API Reference","text":"AcquisitionDomain\n\nSupertype for the set an acquisition is maximized over: a continuous Box (needs a search strategy) or an explicit Points set (argmax over enumerated inputs).\n\n\n\n\n\n","title":"Magpie.AcquisitionDomain"},{"category":"type","location":"api/#Magpie.Box","page":"API Reference","text":"Box(lb, ub) <: AcquisitionDomain\n\nAxis-aligned box domain with lower and upper bounds lb, ub (per-dimension vectors).\n\n\n\n\n\n","title":"Magpie.Box"},{"category":"type","location":"api/#Magpie.Points","page":"API Reference","text":"Points(X) <: AcquisitionDomain\n\nFinite candidate set: the acquisition is evaluated at each input in X and the best is returned. X may be any point container (Vector{Vector}, ColVecs, …).\n\n\n\n\n\n","title":"Magpie.Points"},{"category":"type","location":"api/#Magpie.AcqMaximizer","page":"API Reference","text":"AcqMaximizer\n\nSupertype for strategies that maximize an acquisition over a continuous Box.\n\n\n\n\n\n","title":"Magpie.AcqMaximizer"},{"category":"type","location":"api/#Magpie.SobolPolish","page":"API Reference","text":"SobolPolish{A} <: AcqMaximizer\n\nTwo-stage maximizer for a Box: score n_candidates low-discrepancy Sobol points, then locally polish the best n_restarts of them with box-constrained LBFGS.\n\nConstructor\n\nSobolPolish(; n_candidates=2048, n_restarts=8, ad=AutoForwardDiff())\n\nad selects the DifferentiationInterface backend for the local polish gradients.\n\n\n\n\n\n","title":"Magpie.SobolPolish"},{"category":"function","location":"api/#Magpie.grid_points","page":"API Reference","text":"grid_points(box::Box; per_axis=50) -> Vector\n\nEnumerate a regular grid over box with per_axis points along each dimension.\n\n\n\n\n\n","title":"Magpie.grid_points"},{"category":"function","location":"api/#Magpie.acquire","page":"API Reference","text":"acquire(g, a; over, maximizer=default_for(over))\n\nReturn the input in domain over that maximizes acquisition a under GP g. The maximizer defaults to a sensible strategy for the domain (enumeration for Points, a grid for low-D Boxes, SobolPolish otherwise).\n\n\n\n\n\nacquire(al::ActiveLearner; over, maximizer=default_for(over), q=1)\n\nPick the next query point by maximizing the (resampled) acquisition over over. Batch acquisition (q > 1) is not yet implemented.\n\n\n\n\n\n","title":"Magpie.acquire"},{"category":"type","location":"api/#Magpie.ActiveLearner","page":"API Reference","text":"ActiveLearner{A}\n\nMutable state for an active-learning run: the current GP, the acquisition, and the accumulated query history.\n\nFields\n\ngp: the current AbstractGPModel, replaced on each observation\nacq: the acquisition function of type A\nXs, Ys: accumulated query inputs and observed values, in query order\nacq_vals: the acquisition score at each queried point\n\nConstructor\n\nActiveLearner(gp, acq)\n\nStart a learner from an (optionally unconditioned) GP and an acquisition.\n\n\n\n\n\n","title":"Magpie.ActiveLearner"},{"category":"function","location":"api/#Magpie.observe!","page":"API Reference","text":"observe!(al::ActiveLearner, X, Y) -> al\n\nRecord observation(s) (X, Y) and condition the GP on them. Accepts either a single input/value or a batch (a vector of inputs with a matching vector of values).\n\n\n\n\n\n","title":"Magpie.observe!"},{"category":"function","location":"api/#Magpie.fit!","page":"API Reference","text":"fit!(al::ActiveLearner; restarts=1) -> al\n\nRefit the GP's hyperparameters in place (see fit).\n\n\n\n\n\n","title":"Magpie.fit!"},{"category":"function","location":"api/#Magpie.run!","page":"API Reference","text":"run!(al::ActiveLearner, oracle; budget, over, stop=al->false, refit_every=0) -> al\n\nRun the active-learning loop for up to budget rounds. Each round resamples the acquisition, picks the next point over over, queries oracle for its value, and conditions the GP on it. Stops early when stop(al) is true; refits hyperparameters every refit_every rounds (0 disables refitting).\n\n\n\n\n\n","title":"Magpie.run!"},{"category":"function","location":"api/#Magpie.posterior_gp","page":"API Reference","text":"The current posterior GP held by the learner.\n\n\n\n\n\n","title":"Magpie.posterior_gp"},{"category":"function","location":"api/#Magpie.queried_points","page":"API Reference","text":"Copy of the inputs queried so far, in order.\n\n\n\n\n\n","title":"Magpie.queried_points"},{"category":"function","location":"api/#Magpie.all_data","page":"API Reference","text":"Copies of all queried inputs and their observed values, as (Xs, Ys).\n\n\n\n\n\n","title":"Magpie.all_data"},{"category":"section","location":"generated/levelset_straddle/#Level-set-recovery-with-Straddle","page":"Level-set recovery (Straddle)","text":"This vignette walks through a complete active-learning experiment: recovering the unit-circle level set f(x) = ‖x‖ − 1 = 0 in two dimensions using the Straddle acquisition function.","title":"Level-set recovery with Straddle"},{"category":"section","location":"generated/levelset_straddle/#Setup","page":"Level-set recovery (Straddle)","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, LinearAlgebra, Random\nusing Plots; gr()","title":"Setup"},{"category":"section","location":"generated/levelset_straddle/#What-is-level-set-estimation?","page":"Level-set recovery (Straddle)","text":"Many scientific tasks ask not where is the minimum of f? but where does f change sign? — the boundary of a feasible region, a decision surface, or a phase boundary.  For minimum-finding, mature packages like Surrogates.jl and BayesianOptimization.jl work well. Magpie's Straddle acquisition targets a contour instead — a different question, and a different tool.\n\nThe Straddle criterion (Bryan et al. 2005) is:\n\na(x) = β σ(x) − |μ(x) − h|\n\nwhere h is the target level, μ(x) and σ(x) are the GP posterior mean and standard deviation, and β is a balancing coefficient. High σ pulls the query toward unexplored space; the penalty |μ − h| pulls it toward the boundary.  Together they concentrate observations exactly where the classification decision is most uncertain — near the level set, not at any extremum.","title":"What is level-set estimation?"},{"category":"section","location":"generated/levelset_straddle/#The-function-and-GP-model","page":"Level-set recovery (Straddle)","text":"We use the unit-circle signed-distance function as a clean, visual test case.\n\nRandom.seed!(42)\n\nf(x) = norm(x) - 1.0   ## signed distance to the unit circle\n\nBuild an ActiveLearner with an ExactGP (squared-exponential kernel, lengthscale 0.5, small observation noise) and the Straddle acquisition targeting h = 0.\n\nal = ActiveLearner(\n    ExactGP(with_lengthscale(SqExponentialKernel(), 0.5); noise = 1.0e-4),\n    Straddle(h = 0.0),\n)","title":"The function and GP model"},{"category":"section","location":"generated/levelset_straddle/#Cold-start","page":"Level-set recovery (Straddle)","text":"Seed the GP with 10 random observations spread over the full domain [−2, 2]² so the model has a rough global picture before the acquisition-guided phase begins.\n\nbox = Box([-2.0, -2.0], [2.0, 2.0])\n\nfor x in [4 .* rand(2) .- 2 for _ in 1:10]\n    observe!(al, x, f(x))\nend","title":"Cold start"},{"category":"section","location":"generated/levelset_straddle/#Active-learning-run","page":"Level-set recovery (Straddle)","text":"run! selects 40 further queries, refitting the GP hyperparameters every 10 steps.  The first call to fit! after the cold start sets the kernel parameters; subsequent refits keep them current as new data arrive.\n\nrun!(al, f; budget = 40, over = box, refit_every = 10)","title":"Active-learning run"},{"category":"section","location":"generated/levelset_straddle/#Results","page":"Level-set recovery (Straddle)","text":"g = posterior_gp(al)","title":"Results"},{"category":"section","location":"generated/levelset_straddle/#Figure-1-—-posterior-mean-with-true-contour-and-queries","page":"Level-set recovery (Straddle)","text":"Build a 50×50 evaluation grid and compute the GP posterior mean at each point.  Overlay the true level set (contour! at levels=[0.0]) and mark every point the active learner queried.\n\nxs = range(-2, 2; length = 50)\nys = range(-2, 2; length = 50)\n\nZ_mean = [predmean(g, [xi, yj]) for yj in ys, xi in xs]\nZ_true = [f([xi, yj]) for yj in ys, xi in xs]   ## the true signed-distance field\n\npts = queried_points(al)\npx = [p[1] for p in pts]\npy = [p[2] for p in pts]\n\nplt1 = heatmap(\n    xs, ys, Z_mean;\n    title = \"GP posterior mean\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :RdBu, clim = (-2, 2),\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\n# overlay the TRUE level set (zero contour of f), not the posterior's own contour\ncontour!(plt1, xs, ys, Z_true; levels = [0.0], lw = 2, lc = :black, label = \"true level set (h = 0)\")\nscatter!(\n    plt1, px, py;\n    ms = 4,\n    mc = :white,\n    msw = 1,\n    label = \"queries\",\n)","title":"Figure 1 — posterior mean with true contour and queries"},{"category":"section","location":"generated/levelset_straddle/#Figure-2-—-Straddle-acquisition-surface","page":"Level-set recovery (Straddle)","text":"The acquisition a(g, p) reveals which regions the learner found most informative.  High values (warm colours) lie near the level set where the GP is still uncertain.\n\na = al.acq   ## the Straddle functor, already holds h=0.0\n\nZ_acq = [a(g, [xi, yj]) for yj in ys, xi in xs]\n\nplt2 = heatmap(\n    xs, ys, Z_acq;\n    title = \"Straddle acquisition\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :viridis,\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\n\nThe example's correctness checks run when this file is executed directly (e.g. in CI); they are stripped from this rendered page.\n\n\n\nThis page was generated using Literate.jl.","title":"Figure 2 — Straddle acquisition surface"},{"category":"section","location":"#Magpie.jl","page":"Home","text":"<img class=\"docs-light-only\" src=\"assets/logo.svg\" alt=\"Magpie.jl logo\" width=\"240\"/>\n<img class=\"docs-dark-only\" src=\"assets/logo-dark.svg\" alt=\"Magpie.jl logo\" width=\"240\"/>\n\nComposable Gaussian processes for active learning and dynamics — a GP as a differentiable, uncertainty-aware component.\n\nStatus: v0.1, experimental. The incremental-GP spine and active-learning loop are implemented and tested; the GP-in-SciML bridge is the next milestone. APIs may move.","title":"Magpie.jl"},{"category":"section","location":"#What-it-is","page":"Home","text":"Magpie is a Julia-ecosystem-native Gaussian process package where the bridges are the product. It builds on AbstractGPs.jl and KernelFunctions.jl rather than reinventing the GP core, and treats a GP as a differentiable, uncertainty-aware building block that composes with the rest of Julia — autodiff (Mooncake), SciML, and ML.\n\nTwo capabilities hang off one shared, incrementally-updated GP spine:\n\nActive learning — a composable fit → acquire → observe → update loop with level-set / boundary acquisitions (Straddle, BALD-for-classification) that are not yet available in Julia's GP / Bayesian-optimization packages.\nGP-in-SciML (next milestone) — a GP as the right-hand side of an ODE, trained through the solver, for data-efficient, uncertainty-calibrated dynamics discovery.","title":"What it is"},{"category":"section","location":"#Ecosystem-context","page":"Home","text":"Magpie builds on AbstractGPs.jl and KernelFunctions.jl — it reuses their GP core and kernel library rather than reinventing them.\n\nFor optimization-style infill (expected improvement, upper confidence bound, SRBF), Surrogates.jl and BayesianOptimization.jl are mature choices; they target minima. Magpie's active-learning acquisitions (Straddle, BinaryBALD) target level sets and classification boundaries instead — a different objective these packages don't aim at.\n\nFor GP-as-ODE-field work, GPDiffEq.jl is the original proof of concept and deserves the credit; Magpie's Capability B (not yet built) continues that direction on a through-solver, Mooncake-trained path.","title":"Ecosystem context"},{"category":"section","location":"#Examples","page":"Home","text":"See the Examples section in the navigation sidebar for worked vignettes.","title":"Examples"},{"category":"section","location":"#Install","page":"Home","text":"Not yet registered — add from GitHub:\n\npkg> add https://github.com/jowch/Magpie.jl","title":"Install"}]
}
