var documenterSearchIndex = {"docs":
[{"category":"section","location":"generated/bald_classification/#Data-efficiency-with-BinaryBALD:-learning-curves-on-a-checkerboard","page":"Decision boundary (BinaryBALD)","text":"Active learning earns its keep only when each observation is expensive. A drug-discovery ADMET panel runs roughly $3k–$8k per compound; when labels are cheap and abundant, a larger passively-collected dataset simply wins. But when each query has a real cost, the question is no longer how accurate can we get? but how many queries does it take to reach a target accuracy?\n\nThis vignette makes that question concrete.  We compare two arms on a checkerboard classification task:\n\nBinaryBALD — selects each query to maximally reduce uncertainty about the decision boundary (mutual information between label and model parameters; Houlsby et al. 2011).\nUniform random — draws the next query uniformly at random with no acquisition signal.\n\nThe headline is a learning curve — test accuracy vs number of queries, averaged over 10 paired seeds with ±1 SD bands.","title":"Data efficiency with BinaryBALD: learning curves on a checkerboard"},{"category":"section","location":"generated/bald_classification/#Setup","page":"Decision boundary (BinaryBALD)","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, LinearAlgebra, Random\nusing Statistics: mean, std\nusing Plots; gr()","title":"Setup"},{"category":"section","location":"generated/bald_classification/#The-task:-a-checkerboard-decision-boundary","page":"Decision boundary (BinaryBALD)","text":"We classify points in [−2, 2]² by the sign of sin(2x₁)·sin(2x₂). This produces an interleaved checkerboard pattern with four sign-alternating cells in each quadrant — genuinely harder than a convex boundary because the learner must resolve multiple disconnected regions simultaneously.  That multi-region structure is exactly what rewards informative sampling most.\n\nlabel(x) = sin(2x[1]) * sin(2x[2]) > 0\n\nbox = Box([-2.0, -2.0], [2.0, 2.0])","title":"The task: a checkerboard decision boundary"},{"category":"section","location":"generated/bald_classification/#Learning-curves:-methodology","page":"Decision boundary (BinaryBALD)","text":"A held-out test set of 400 random points is fixed once (seed 0) and reused across every run.  Test accuracy is the idiomatic AL metric: it measures generalisation to unseen points rather than memorisation of training data.\n\nFor each of 10 seeds we run both arms from the same cold start (10 random points drawn with that seed), then step each arm one query at a time up to budget 40.  Sharing the cold start ensures a fair paired comparison. Averaging over 10 seeds gives stable mean curves and honest ±1 SD bands — the reproducibility literature shows single-run AL claims are often noise.\n\nThe classifier's lengthscale is now fitted: every 10 queries we re-optimize the Laplace evidence to adapt the kernel to the accumulated data.\n\n# Fixed held-out test set, seeded once\nRandom.seed!(0)\nXtest = [4 .* rand(2) .- 2 for _ in 1:400]\nytest = label.(Xtest)\nacc(g) = sum((predmean(g, p) > 0) == yt for (p, yt) in zip(Xtest, ytest)) / length(Xtest)\n\nfunction learning_curve(seed; bald::Bool, B = 40, ℓ = 0.4, refit_every = 10)\n    Random.seed!(seed)\n    cold = [4 .* rand(2) .- 2 for _ in 1:10]    # paired: same seed ⇒ identical cold start for both arms\n    al = ActiveLearner(LaplaceGP(with_lengthscale(SqExponentialKernel(), ℓ)), BinaryBALD())\n    for x in cold\n        observe!(al, x, label(x))\n    end\n    accs = Float64[]\n    for t in 1:B\n        x = bald ? acquire(al; over = box) : (4 .* rand(2) .- 2)   # BALD-selected vs uniform-random\n        observe!(al, x, label(x))\n        refit_every > 0 && t % refit_every == 0 && fit!(al)\n        push!(accs, acc(posterior_gp(al)))\n    end\n    return accs\nend\n\nSEEDS = 1:10\nbald_curves = [learning_curve(s; bald = true)  for s in SEEDS]\nrand_curves = [learning_curve(s; bald = false) for s in SEEDS]\nmb = [mean(c[k] for c in bald_curves) for k in 1:40]   # mean BALD curve\nmr = [mean(c[k] for c in rand_curves) for k in 1:40]   # mean random curve\nsb = [std(c[k]  for c in bald_curves) for k in 1:40]   # ±1 SD for ribbons\nsr = [std(c[k]  for c in rand_curves) for k in 1:40]","title":"Learning curves: methodology"},{"category":"section","location":"generated/bald_classification/#Figure-1-—-the-headline-learning-curve","page":"Decision boundary (BinaryBALD)","text":"BinaryBALD reaches 80% test accuracy around query 30 on average; uniform random does not cross that threshold within the 40-query budget.  The ±1 SD bands show the variation across seeds: the gap is consistent, not a lucky draw.\n\nWhy?  Uniform random spreads its budget across the whole domain, wasting queries in regions far from any decision boundary.  BinaryBALD's mutual-information criterion spends each query where it most reduces uncertainty about the boundary, compressing the path to the target accuracy.\n\nCaveats (important).  AL assumes a reasonably calibrated model.  At very low budgets the posterior is noisy and BALD can match or trail random; and if observations are genuinely cheap and plentiful, passive sampling wins.  A natural additional baseline — not shown here — is max-entropy / uncertainty sampling (query wherever σ(x) is largest); that is a small acquisition to add and would provide a tighter comparison than uniform random alone.\n\nplt1 = plot(\n    mb;\n    ribbon = sb,\n    label = \"BinaryBALD\",\n    xlabel = \"queries (after cold start)\",\n    ylabel = \"test accuracy\",\n    title = \"Data efficiency: BinaryBALD vs random\",\n    legend = :bottomright,\n    ylims = (0.5, 1.0),\n    linewidth = 2,\n)\nplot!(\n    plt1, mr;\n    ribbon = sr,\n    label = \"uniform random\",\n    linewidth = 2,\n)\nhline!(\n    plt1, [0.8];\n    ls = :dash,\n    lc = :black,\n    lw = 1,\n    label = \"80% target\",\n)","title":"Figure 1 — the headline learning curve"},{"category":"section","location":"generated/bald_classification/#Figure-2-—-snapshot-at-budget-40-(seed-1)","page":"Decision boundary (BinaryBALD)","text":"The latent GP mean predmean(g, p) is the pre-sigmoid score: positive in label = true cells, negative in label = false cells.  Blue marks the positive class, red the negative; the true checkerboard boundaries are overlaid in black.  White dots are the 40 BALD queries (after the 10-point cold start).\n\n# Reproduce seed-1 BALD run for the snapshot\nRandom.seed!(1)\ncold1 = [4 .* rand(2) .- 2 for _ in 1:10]\nal1 = ActiveLearner(LaplaceGP(with_lengthscale(SqExponentialKernel(), 0.4)), BinaryBALD())\nfor x in cold1\n    observe!(al1, x, label(x))\nend\nrun!(al1, label; budget = 40, refit_every = 10, over = box)\ng1 = posterior_gp(al1)\n@info \"fitted lengthscale\" ℓ = round(Magpie._lengthscale(g1.prior.kernel); digits = 3)\n\nxs = range(-2, 2; length = 50)\nys = range(-2, 2; length = 50)\n\nZ_mean = [predmean(g1, [xi, yj]) for yj in ys, xi in xs]\n\n# True checkerboard boundaries: contour of sin(2x₁)·sin(2x₂) at level 0\nZ_check = [sin(2xi) * sin(2yj) for yj in ys, xi in xs]\n\npts1 = queried_points(al1)\npx1 = [p[1] for p in pts1]\npy1 = [p[2] for p in pts1]\n\nplt2 = heatmap(\n    xs, ys, Z_mean;\n    title = \"Latent posterior mean, seed 1 (40 BALD queries)\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :RdBu,\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\ncontour!(plt2, xs, ys, Z_check; levels = [0.0], lw = 2, lc = :black, label = \"true boundary\")\nscatter!(\n    plt2, px1, py1;\n    ms = 4,\n    mc = :white,\n    msw = 1,\n    label = \"BALD queries\",\n)\n\nThe example's correctness checks run when this file is executed directly (e.g. in CI); they are stripped from this rendered page.\n\n\n\nThis page was generated using Literate.jl.","title":"Figure 2 — snapshot at budget 40 (seed 1)"},{"category":"section","location":"generated/gp_ude_vanderpol/#GP-UDE:-Van-der-Pol-oscillator","page":"GP-UDE: Van der Pol oscillator","text":"Learn the full nonlinear vector field of a Van der Pol relaxation oscillator (μ=1.5) from noisy observations.\n\nA single trajectory over (0, 3) with observation noise σ_obs ≈ 0.03 is used to train an ExactGPField end-to-end via train! (ADAM warm-up → LBFGS polish). Recovery is measured three honest ways:\n\nTrajectory RMSE — ODE integration of the GP posterior mean field vs the clean ground truth.  PULL is the uncertainty propagator, not the mean-trajectory integrator: the Euler recurrence in PULL accumulates much larger errors than a proper ODE solver on the same mean field.\nField error — GP vector field vs true RHS, on-trajectory and off-manifold (grid around the limit cycle).\nHeld-out-IC coverage — propagate uncertainty from a fresh initial condition and compare the Pathwise ensemble against the clean held-out trajectory.  Two propagators are compared:\nPULL — a cheap analytic moment-matching propagator. Its mean uses a first-order Euler recurrence, which drifts off the true trajectory on a nonlinear oscillator; coverage collapses to ~0. This is a documented Euler limitation of PULL, not a failure of the learned field.\nPathwise — a Monte-Carlo ensemble of decoupled GP samples, each integrated as a proper ODE. For the Van der Pol oscillator the GP posterior is broad off-trajectory (the field is learned only from one single-shooting training trajectory); Pathwise coverage against a held-out IC is therefore informational — it quantifies how the GP uncertainty expands when extrapolating to nearby initial conditions. For this example the hard gates are trajectory RMSE and on-trajectory field error (metrics 1 and 2).\n\nSolver note: train! accepts solver=/sensealg= kwargs forwarded to every in-loop ODE solve. The default Tsit5() pairs cleanly with GaussAdjoint+MooncakeVJP; for strongly stiff systems (μ≫1) you would pass solver=AutoTsit5(Rosenbrock23()) together with sensealg=InterpolatingAdjoint(autojacvec=ReverseDiffVJP(true)). At μ=1.5 the system is only mildly stiff and Tsit5 is the honest choice. (propagate's Pathwise integrators are fixed at Tsit5(); PULL uses no ODE solver.)\n\nNone of these metrics is in-sample loss.\n\nENV[\"GKSwstype\"] = \"100\"  ## GR headless\nusing Magpie, OrdinaryDiffEq, SciMLSensitivity, KernelFunctions, LinearAlgebra, Random\nusing Statistics\nusing Plots; gr()","title":"GP-UDE: Van der Pol oscillator"},{"category":"section","location":"generated/gp_ude_vanderpol/#True-system-—-Van-der-Pol-oscillator-(μ1.5)","page":"GP-UDE: Van der Pol oscillator","text":"function vdp!(du, u, p, t)\n    μ = 1.5\n    du[1] = u[2]\n    du[2] = μ * (1 - u[1]^2) * u[2] - u[1]\n    return nothing\nend\n\nvdp_true(u) = [u[2], 1.5 * (1 - u[1]^2) * u[2] - u[1]]\n\nu0 = [2.0, 0.0]\ntspan = (0.0, 3.0)\nts = collect(range(tspan...; length = 20))\n\nClean training trajectory.\n\ntarget = Array(solve(ODEProblem(vdp!, u0, tspan), Tsit5(); saveat = ts))","title":"True system — Van der Pol oscillator (μ=1.5)"},{"category":"section","location":"generated/gp_ude_vanderpol/#Add-observation-noise-(σ*obs*true-0.03)","page":"GP-UDE: Van der Pol oscillator","text":"σ_obs_true = 0.03f0\nrng_noise = MersenneTwister(123)\nXnoisy = target .+ σ_obs_true .* randn(rng_noise, size(target))","title":"Add observation noise (σobstrue ≈ 0.03)"},{"category":"section","location":"generated/gp_ude_vanderpol/#Build-and-train-the-GP-UDE-field-on-NOISY-data","page":"GP-UDE: Van der Pol oscillator","text":"Random.seed!(42)\nZ = kmeans_anchors(Xnoisy, 12; rng = MersenneTwister(7))\n\nlogℓ0=nothing ⇒ data-driven median-heuristic lengthscale init (starts the optimizer on the data scale; see CLAUDE.md on GP-UDE multi-basin / BLAS sensitivity).\n\nfield = ExactGPField(SqExponentialKernel(), Z; d = 2, logℓ0 = nothing)\n\nλ=1/(20*2) is a weak log-ℓ prior centred at 0 with std 0.5. We train on Xnoisy — the field must learn through noise, not a clean signal.\n\ntrain!(field, (ts, Xnoisy); tspan, maxiters = 150, λ = 1 / (20 * 2), s = 0.5)","title":"Build and train the GP-UDE field on NOISY data"},{"category":"section","location":"generated/gp_ude_vanderpol/#Posterior-GPs","page":"GP-UDE: Van der Pol oscillator","text":"g = posterior(field)   # ONE multi-output GP (d=2)","title":"Posterior GPs"},{"category":"section","location":"generated/gp_ude_vanderpol/#1.-Trajectory-RMSE-—-ODE-integration-of-the-GP-mean-field-vs-clean-truth","page":"GP-UDE: Van der Pol oscillator","text":"PULL propagation uses a first-order Euler step for the mean, which accumulates large errors on a nonlinear oscillator.  For the mean-trajectory metric we integrate the GP posterior mean field as a proper ODE (same as what training minimises), then compare against the clean truth.\n\ngp_rhs!(du, u, p, t) = (du .= vec(mean(g, [u])); nothing)\nsol_gp = Array(solve(ODEProblem(gp_rhs!, u0, tspan), Tsit5(); saveat = ts))\n\ntraj_pred = [sol_gp[:, i] for i in 1:length(ts)]\ntraj_truth = [target[:, i] for i in 1:length(ts)]\n\nOff-manifold test grid: 10×10 around the limit cycle.\n\nu1_grid = range(-2.5, 2.5; length = 10)\nu2_grid = range(-3.0, 3.0; length = 10)\noffpts = vec([[p1, p2] for p1 in u1_grid, p2 in u2_grid])\n\nmetrics = recovery_metrics(g, vdp_true, traj_pred, traj_truth; offpts = offpts)\n\n@info \"Trajectory RMSE (ODE integration of GP mean vs clean truth)\" metrics.traj_rmse\n@info \"Field error (on-trajectory)\"  metrics.field_err_visited.median  metrics.field_err_visited.q90\n@info \"Field error (off-manifold)\"   metrics.field_err_offmanifold.median  metrics.field_err_offmanifold.q90","title":"1. Trajectory RMSE — ODE integration of the GP mean field vs clean truth"},{"category":"section","location":"generated/gp_ude_vanderpol/#2.-Plot:-mean-trajectory-vs-clean-data","page":"GP-UDE: Van der Pol oscillator","text":"p1 = plot(ts, target[1, :], label = \"x (clean)\", lw = 2, c = :blue)\nplot!(p1, ts, target[2, :], label = \"ẋ (clean)\", lw = 2, c = :red)\nplot!(p1, ts, Xnoisy[1, :], label = \"x (noisy data)\", lw = 1, c = :blue, ls = :dot, alpha = 0.6)\nplot!(p1, ts, Xnoisy[2, :], label = \"ẋ (noisy data)\", lw = 1, c = :red, ls = :dot, alpha = 0.6)\nplot!(p1, ts, sol_gp[1, :], label = \"x (GP ODE mean)\", lw = 2, c = :blue, ls = :dash)\nplot!(p1, ts, sol_gp[2, :], label = \"ẋ (GP ODE mean)\", lw = 2, c = :red, ls = :dash)\nxlabel!(p1, \"t\"); ylabel!(p1, \"state\")\ntitle!(p1, \"VdP GP-UDE: mean trajectory (trained on noisy data)\")\nsavefig(p1, \"vdp_trajectory.png\")","title":"2. Plot: mean trajectory vs clean data"},{"category":"section","location":"generated/gp_ude_vanderpol/#3.-Held-out-initial-condition-—-uncertainty-propagation-(informational)","page":"GP-UDE: Van der Pol oscillator","text":"We propagate uncertainty from a fresh initial condition two ways and compare coverage of the clean held-out trajectory: PULL (cheap analytic, Euler-limited) and Pathwise (Monte-Carlo ensemble of decoupled GP samples).\n\nCoverage context: The GP is trained on a single trajectory starting at u0=[2,0]. It extrapolates to a nearby held-out IC via its kernel's smoothness. The Pathwise ensemble covers the field's uncertainty about the VdP dynamics near the held-out IC, but single-shooting over a 3-second nonlinear oscillator amplifies that uncertainty substantially. Coverage is reported as an informational diagnostic, not a hard gate; the hard gates are metrics 1 (trajectory RMSE) and 2 (field error).\n\nu0_test = [2.0, 0.5]   # NOT the training IC\nts_test = collect(range(tspan...; length = 20))\n\nClean ground-truth trajectory from the held-out IC.\n\ntarget_test = Array(solve(ODEProblem(vdp!, u0_test, tspan), Tsit5(); saveat = ts_test))\ntruth_vecs = [target_test[:, i] for i in 1:length(ts_test)]","title":"3. Held-out initial condition — uncertainty propagation (informational)"},{"category":"section","location":"generated/gp_ude_vanderpol/#3a.-PULL-—-cheap-analytic-propagation-(Euler-limited-on-nonlinear-horizons)","page":"GP-UDE: Van der Pol oscillator","text":"PULL's mean is a first-order Euler recurrence; on a nonlinear oscillator it drifts from the true trajectory, so coverage collapses to ~0. Kept as a documented contrast, NOT as the validated-uncertainty story.\n\nμs_test, Σs_test = propagate(g, u0_test, tspan; method = PULL(), ts = ts_test)\ncov90_pull = coverage(truth_vecs, μs_test, Σs_test; level = 0.9)\n\n@info \"Held-out-IC PULL coverage at 90% nominal (Euler-limited)\" cov90_pull","title":"3a. PULL — cheap analytic propagation (Euler-limited on nonlinear horizons)"},{"category":"section","location":"generated/gp_ude_vanderpol/#3b.-Pathwise-—-Monte-Carlo-ensemble-of-decoupled-GP-samples","page":"GP-UDE: Van der Pol oscillator","text":"Each of n samples is a decoupled GP draw integrated as a proper ODE, so the ensemble carries the field's uncertainty without PULL's Euler drift. propagate(...; method=Pathwise(n=N)) returns an N × d × |ts| array.\n\nens = propagate(g, u0_test, tspan; method = Pathwise(n = 128), ts = ts_test)\n\nPer-step empirical mean + covariance from the ensemble, then reuse the same Mahalanobis-χ² coverage as PULL (apples-to-apples at 90% nominal). Skip the first step (all samples start at the same u0_test; zero variance).\n\nnsteps = length(ts_test)\nμs_path, Σs_path = pathwise_moments(ens)\n\nCoverage over steps 2:end only (step 1 is deterministic: all samples = u0_test).\n\ncov90_path = coverage(truth_vecs[2:end], μs_path[2:end], Σs_path[2:end]; level = 0.9)\n\n@info \"Held-out-IC Pathwise coverage at 90% nominal (informational)\" cov90_path","title":"3b. Pathwise — Monte-Carlo ensemble of decoupled GP samples"},{"category":"section","location":"generated/gp_ude_vanderpol/#4.-Plot:-Pathwise-ensemble-band-over-the-clean-held-out-trajectory","page":"GP-UDE: Van der Pol oscillator","text":"5th–95th percentile envelope (90% band) per dimension across the n samples, overlaid on the clean held-out truth.\n\nu1_lo = [quantile(ens[:, 1, k], 0.05) for k in 1:nsteps]\nu1_hi = [quantile(ens[:, 1, k], 0.95) for k in 1:nsteps]\nu2_lo = [quantile(ens[:, 2, k], 0.05) for k in 1:nsteps]\nu2_hi = [quantile(ens[:, 2, k], 0.95) for k in 1:nsteps]\nμmat_path = reduce(hcat, μs_path)   # 2 × |ts_test|\n\np2 = plot(ts_test, target_test[1, :], label = \"x (clean, held-out IC)\", lw = 2, c = :blue)\nplot!(p2, ts_test, target_test[2, :], label = \"ẋ (clean, held-out IC)\", lw = 2, c = :red)\nplot!(p2, ts_test, μmat_path[1, :], label = \"x μ (Pathwise)\", lw = 2, c = :blue, ls = :dash)\nplot!(p2, ts_test, μmat_path[2, :], label = \"ẋ μ (Pathwise)\", lw = 2, c = :red, ls = :dash)\n\n5–95% Pathwise envelopes.\n\nplot!(p2, ts_test, u1_hi, fillrange = u1_lo, alpha = 0.15, c = :blue, label = \"x 5–95%\", lw = 0)\nplot!(p2, ts_test, u2_hi, fillrange = u2_lo, alpha = 0.15, c = :red, label = \"ẋ 5–95%\", lw = 0)\nxlabel!(p2, \"t\"); ylabel!(p2, \"state\")\ntitle!(p2, \"VdP GP-UDE: held-out-IC Pathwise ensemble band (90%)\")\nsavefig(p2, \"vdp_trajectory_coverage.png\")","title":"4. Plot: Pathwise ensemble band over the clean held-out trajectory"},{"category":"section","location":"generated/gp_ude_vanderpol/#Anti-rot-assertions-(#src-lines-run-on-direct-execution-only)","page":"GP-UDE: Van der Pol oscillator","text":"GP-UDE training is multi-basin and BLAS-sensitive (see CLAUDE.md), so exact recovery RMSE / field error vary with the backend; the gates below assert API/structural invariants + a loose blow-up bound, and the recovery numbers are reported via @info and shown in the rendered docs (Julia 1.12). Coverage is @info'd only: single-shooting a nonlinear oscillator amplifies field uncertainty, so Pathwise coverage of a held-out IC is informational, not a calibration gate; PULL collapses to ~0 by design (Euler drift on a nonlinear system is a documented PULL limitation).\n\n\n\nThis page was generated using Literate.jl.","title":"Anti-rot assertions (#src lines run on direct execution only)"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"The public surface of Magpie.jl. Names are grouped by role; see the Examples for worked usage.","title":"API Reference"},{"category":"section","location":"api/#Models-and-inference","page":"API Reference","text":"","title":"Models and inference"},{"category":"section","location":"api/#Acquisitions","page":"API Reference","text":"","title":"Acquisitions"},{"category":"section","location":"api/#Maximizing-an-acquisition","page":"API Reference","text":"","title":"Maximizing an acquisition"},{"category":"section","location":"api/#Transition-state-search","page":"API Reference","text":"","title":"Transition-state search"},{"category":"section","location":"api/#Active-learning-loop","page":"API Reference","text":"","title":"Active-learning loop"},{"category":"type","location":"api/#Magpie.AbstractGPModel","page":"API Reference","text":"AbstractGPModel <: AbstractGPs.AbstractGP\n\nContract for a GP that plugs into Magpie's active-learning loop and SciML bridge.\n\nSubtyping AbstractGPs.AbstractGP inherits the Distributions interface (f(x) → FiniteGP, rand, logpdf, mean/var/cov). On top of that, an AbstractGPModel is expected to implement the extended contract the loop drives:\n\nupdate(g, X, y) — incremental conditioning on new observations\nfit(g; …) — hyperparameter optimization\npredmean(g, u) — posterior mean at a single point\n\nSubtype this and implement those methods to plug in a new GP (e.g. a sparse GP). Bundled implementations: ExactGP, LaplaceGP.\n\n\n\n\n\n","title":"Magpie.AbstractGPModel"},{"category":"type","location":"api/#Magpie.ExactGP","page":"API Reference","text":"ExactGP{Tp,Tx,Tδ,TC,Tα} <: AbstractGPModel\n\nExact-regression GP holding the cached state of an incremental conditioning. The posterior mean reuses the cached weights alpha:\n\nmu(x_ast) = m(x_ast) + mathrmcov(x_ast x)alpha\nqquad alpha = C^-1delta\n\nField names follow Rasmussen & Williams (Algorithm 2.1).\n\nFields\n\nprior: the prior AbstractGPs.GP (mean + kernel)\nx: training inputs\nδ: residuals delta = y - m(x) against the prior mean\nC: Cholesky factorization of K + sigma^2 I, extended in place on each update\nα: cached weights alpha = C^-1delta\nnoise: observation-noise variance sigma^2 added to the kernel diagonal\n\nConstructor\n\nExactGP(kernel::Kernel; noise=1e-6, mean=AbstractGPs.ZeroMean())\n\nBuild an unconditioned ExactGP; condition it on data with update.\n\n\n\n\n\n","title":"Magpie.ExactGP"},{"category":"type","location":"api/#Magpie.LaplaceGP","page":"API Reference","text":"LaplaceGP{Tp,Tx,Ty,Ta,TW,TL} <: AbstractGPModel\n\nBinary-classification GP using the Laplace approximation (Rasmussen & Williams, Algorithm 3.1) with a logistic likelihood. The latent posterior is approximated by a Gaussian centred at the MAP hatf with precision\n\nSigma^-1 = K^-1 + W qquad B = I + sqrtWKsqrtW\n\nwhere W is the (diagonal) likelihood Hessian and B is the well-conditioned matrix actually factorized. The MAP is re-fit from scratch on each update, so the full observation history is retained. Field names follow R&W Algorithm 3.1.\n\nFields\n\nprior: the prior AbstractGPs.GP over the latent function f\nx: training inputs\ny: Bool class labels, kept so re-fitting can use the full history\na: dual vector K^-1(hatf - m) at the MAP latent hatf\nW: diagonal of the likelihood Hessian, W_i = hatpi_i(1 - hatpi_i), at the MAP\nL: Cholesky factor of B = I + sqrtWKsqrtW at the MAP\n\nConstructor\n\nLaplaceGP(kernel::Kernel; mean=AbstractGPs.ZeroMean())\n\nBuild an unconditioned LaplaceGP; condition it on labels with update.\n\n\n\n\n\n","title":"Magpie.LaplaceGP"},{"category":"function","location":"api/#Magpie.update","page":"API Reference","text":"update(g::ExactGP, X, y) -> ExactGP\n\nCondition the GP on observations (X, y), returning a new conditioned ExactGP.\n\nThe first call factorizes K + σ²I from scratch; later calls extend the existing Cholesky factor incrementally via AbstractGPs.update_chol. A scalar y conditions on a single point.\n\n\n\n\n\nupdate(g::LaplaceGP, X, y) -> LaplaceGP\n\nCondition the classifier on new labelled inputs (X, y). Because the Laplace MAP is re-fit from scratch, the new data is appended to the stored history and the MAP solve (_laplace_fit) is rerun over the full set.\n\nLabels y may be Bool, {0,1} integers, or {-1,+1} integers; they are coerced to Bool before fitting.\n\n\n\n\n\n","title":"Magpie.update"},{"category":"function","location":"api/#Magpie.predict","page":"API Reference","text":"predict(g, xs) -> (mean, var)\n\nPosterior mean and marginal variance at the inputs xs (an alias for mean_and_var on any AbstractGP).\n\nFor a d>1 GP, the mean is an nx×d matrix (one column per output) and the variance is a length-nx vector shared across outputs (it depends only on the training inputs x, not on the observed values).\n\n\n\n\n\n","title":"Magpie.predict"},{"category":"function","location":"api/#Magpie.predmean","page":"API Reference","text":"predmean(g::ExactGP, u) -> Real\n\nPosterior mean at a single input u, returned as a scalar (unlike mean(g, [u]), which returns a length-1 vector). Only supported for single-output GPs (d=1); throws ArgumentError for d>1.\n\n\n\n\n\nPosterior latent mean at a single input u, as a scalar; >0 predicts the positive class.\n\n\n\n\n\nPosterior mean at a single input u (scalar). Single-output (d=1) only — d>1 throws.\n\n\n\n\n\n","title":"Magpie.predmean"},{"category":"function","location":"api/#Magpie.grad_predict","page":"API Reference","text":"grad_predict(g::ExactGP, x; hessian=true) -> (μ∇, Σdiag, H)\n\nPosterior gradient mean μ∇ (length d), marginal gradient variances Σdiag (length d, one per ∂f/∂xᵢ), and posterior-mean Hessian H (d×d) of an ExactGP at query x, observing f-values only.\n\nKernel-generic: μ∇ and H are AD (ForwardDiff) of the posterior mean predmean, so they need no hand-derived per-kernel algebra and pick up the kernel output scale σf² for free. Only the prior gradient-variance term needs kernel-specific care (see [`priorgradvar`](@ref)).\n\nMath: with the posterior mean μ(x)=m(x)+k(x,X)α,   μ∇  = ∇ₓ μ(x),  H = ∇²ₓ μ(x),   Var[∂ᵢf] = Var_prior[∂ᵢf] − (∂ᵢk(x,X)) C⁻¹ (∂ᵢk(x,X))ᵀ.\n\nponytail: AD path, kernel-general. Slower than hand-rolled RBF blocks (n kernel-grad\n\ncalls for the variance, a d-dual Hessian only at classification time), but the thesis is\n\nto bridge KernelFunctions + AD, not hand-code derivatives per kernel. The acquisition\n\nuses hessian=false; the AD Hessian fires only at the handful of extracted candidates.\n\n\n\n\n\n","title":"Magpie.grad_predict"},{"category":"function","location":"api/#Magpie.nlml","page":"API Reference","text":"nlml(g::ExactGP) -> Real\n\nNegative log marginal likelihood, the objective fit minimizes:\n\n-log p(y mid X) = tfrac12delta^top alpha\n    + sum_i log C_ii + tfracn2log 2pi\n\nwhere delta = y - m(X), alpha = C^-1delta, and C is the upper Cholesky factor of K + sigma^2 I. Returns 0.0 for an unconditioned GP.\n\n\n\n\n\nnlml(g::LaplaceGP) -> Real\n\nNegative Laplace log marginal likelihood (Rasmussen & Williams, eq. 3.32) — the objective fit minimizes for a LaplaceGP:\n\n-log q(y mid X) = tfrac12(hat f - m)^top a - log p(y mid hat f)+sum_i log L_ii\n\nwhere hat f = K a + m is the MAP latent, a the cached dual, and L the Cholesky factor of B = I + W^12 K W^12. Returns 0.0 for an unconditioned classifier.\n\n\n\n\n\n","title":"Magpie.nlml"},{"category":"function","location":"api/#Magpie.fit","page":"API Reference","text":"fit(g::AbstractGPModel; restarts=1, ad=nothing, ℓ_prior=:auto) -> AbstractGPModel\n\nOptimize the kernel's hyperparameters by minimizing nlml (plus a lengthscale prior; see below) with LBFGS, returning a GP re-conditioned at the best hyperparameters. Works for any AbstractGPModel that defines nlml, _fit_xy, and _recondition — ExactGP (exact evidence) and LaplaceGP (Laplace evidence).\n\nfit treats the GP's kernel as a structural template: it Optimisers.destructures it into a flat vector of positive scale parameters (inverse-lengthscales and output scales), optimizes the log of that vector (bounded to [-6, 6], so every scale stays positive), and rebuilds the kernel. This supports any KernelFunctions kernel whose hyperparameters are positive scales: SqExponential/Matern/RationalQuadratic bases, ARD (a lengthscale per input dimension), and sums/products of these. A bare with_lengthscale(base, ℓ) kernel (no signal-variance factor) is auto-wrapped as 1.0 * k so σf² is always a tunable leaf — fitting σf² calibrates the function scale, which the derivative/straddle acquisitions need.\n\nad selects the DifferentiationInterface backend; nothing (default) picks automatically: AutoForwardDiff() for SqExponentialKernel bases (fast, smooth at r=0) and AutoMooncake() otherwise (Matérn bases NaN under ForwardDiff at coincident points via sqrt(0); composites default to Mooncake too). With restarts > 1, extra runs start from the initial point jittered in log-space and the result with the lowest penalized objective wins.\n\nLengthscale prior (MAP, default on — scalar-lengthscale kernels only)\n\nFor a single-scalar-lengthscale kernel, fit is MAP: it adds a weakly-informative Gaussian prior on logℓ, 0.5·((logℓ − μ)/σ)². ℓ_prior=:auto (default) centres it on the initial lengthscale of g's kernel with width σ=0.75 (log units) — refine the lengthscale you specified, don't run away from it. This matters when data is scarce: pure MLE drives ℓ up (a flat surface explains few points cheaply), over-smoothing away the wells/saddles one hunts. Pass ℓ_prior=(μ, σ) to set centre/width explicitly, or ℓ_prior=nothing for pure MLE. σ_f² is never penalized.\n\nFor ARD or composite kernels there is no single lengthscale, so ℓ_prior=:auto falls back to no prior (pure MLE); passing an explicit ℓ_prior=(μ,σ) then raises an ArgumentError.\n\nnote: Note\nfit requires the kernel's hyperparameters to be positive scales (lengthscales, output scales). Kernels with non-positive or non-scale leaves (e.g. LinearKernel, whose offset destructures to 0.0) raise an ArgumentError.\n\n\n\n\n\n","title":"Magpie.fit"},{"category":"type","location":"api/#Magpie.AcquisitionFunction","page":"API Reference","text":"AcquisitionFunction\n\nSupertype for active-learning acquisitions. An acquisition is callable as a(g, x), scoring a candidate input x under the current GP g; the loop queries where the score is largest (see acquire).\n\n\n\n\n\n","title":"Magpie.AcquisitionFunction"},{"category":"type","location":"api/#Magpie.MarginalAcquisition","page":"API Reference","text":"MarginalAcquisition <: AcquisitionFunction\n\nAcquisitions that depend only on the GP's marginal posterior at x (its mean and variance), so each candidate can be scored independently.\n\n\n\n\n\n","title":"Magpie.MarginalAcquisition"},{"category":"type","location":"api/#Magpie.Straddle","page":"API Reference","text":"Straddle{T} <: MarginalAcquisition\n\nLevel-set \"straddle\" acquisition (Bryan et al. 2005):\n\na(x) = betasigma(x) - mu(x) - h\n\nIt is large where the posterior is uncertain (sigma high) and close to the target level h, driving queries onto the level set x  f(x) = h.\n\nConstructor\n\nStraddle(; h=0.0, β=1.96)\n\nh is the target level and β the exploration weight (the default ≈ a 95% band).\n\n\n\n\n\n","title":"Magpie.Straddle"},{"category":"type","location":"api/#Magpie.RandStraddle","page":"API Reference","text":"RandStraddle{T,R} <: MarginalAcquisition\n\nRandomized straddle (Inatsu et al. 2024): like Straddle but the band width β is redrawn each round as √(−2 log u), u ~ U(0,1), giving a theoretically grounded exploration schedule. Call resample between rounds to redraw.\n\nConstructor\n\nRandStraddle(; h=0.0, rng=Random.default_rng())\n\nh is the target level; rng seeds the band-width draw. The field sβ stores the current √β.\n\n\n\n\n\n","title":"Magpie.RandStraddle"},{"category":"type","location":"api/#Magpie.BinaryBALD","page":"API Reference","text":"BinaryBALD <: AcquisitionFunction\n\nBayesian Active Learning by Disagreement for binary classification (Houlsby et al. 2011): the mutual information between the label y and the latent function f,\n\nmathbbIy f mid x mathcalD\n    = mathrmHy mid x mathcalD - mathbbE_fmathrmHy mid f\n\ncomputed in bits. Large where the GP is uncertain about the class probability, driving queries that are most informative about the decision boundary. Pair with a LaplaceGP.\n\n\n\n\n\n","title":"Magpie.BinaryBALD"},{"category":"type","location":"api/#Magpie.GradStraddle","page":"API Reference","text":"GradStraddle(; β=1.96) <: AcquisitionFunction\n\nVector-zero straddle: a component-wise Straddle on the GP gradient, summed over components. Scores high where every ∂f/∂xᵢ is near zero AND uncertain, so the loop samples toward the zeros of ∇f (the critical points of f).\n\ntextscore(x) = sum_i left betasqrtmathrmVarpartial_i f(x) - mathbbEpartial_i f(x) right\n\nSum (not min) over components: min is dominated by the most-resolved component and starves half-resolved critical points; the sum keeps explore/exploit tension on every component.\n\n\n\n\n\n","title":"Magpie.GradStraddle"},{"category":"type","location":"api/#Magpie.RandGradStraddle","page":"API Reference","text":"RandGradStraddle{R} <: AcquisitionFunction\n\nRandomized GradStraddle: the band width is redrawn each round as √(−2 log u), u ~ U(0,1) (the RandStraddle schedule). Randomizing the band breaks the deterministic-argmax fixation that mode-collapses the plain GradStraddle on landscapes with large gradient-magnitude variation — there the constant −|μ∇| term pins the maximizer to one region, so a single-scale loop covers it but a steep multiscale one does not. Call resample between rounds to redraw.\n\n\n\n\n\n","title":"Magpie.RandGradStraddle"},{"category":"type","location":"api/#Magpie.LocalPenalization","page":"API Reference","text":"LocalPenalization(base, pts=[]; c=0.5, s=0.15) <: AcquisitionFunction\n\nDiversity wrapper around any base acquisition: discourages querying within sim cell of any point in pts (the observation history; ell is the GP's fitted lengthscale). Each point adds a soft penalty to the score,\n\nsum_j log Phileft(fraclVert x - x_jrVert - cellsellright)\n\nwhich is approx 0 far from xⱼ and strongly negative inside the radius. This breaks the deterministic-argmax resampling collapse — stacking queries at one point never resolves the gradient there, so a plain straddle can sample it indefinitely and starve coverage.\n\nIn the active loop, pass the learner's live history so the penalty tracks new observations with no loop changes (observe! mutates al.Xs in place):\n\nal.acq = LocalPenalization(al.acq, al.Xs)\n\nAdapts Local Penalization (González et al. 2016, Batch Bayesian Optimization via Local Penalization) to gradient-zero / level-set search: the soft Phi envelope is the same, but the radius is the lengthscale cell (a redundancy scale) rather than LP's optimum-seeking (M-mu)L, which assumes a scalar being maximized — see the package notes for why that radius does not transfer to this objective.\n\n\n\n\n\n","title":"Magpie.LocalPenalization"},{"category":"function","location":"api/#Magpie.resample","page":"API Reference","text":"resample(a::AcquisitionFunction[, rng]) -> AcquisitionFunction\n\nReturn a copy of a with any per-round randomness redrawn. Stochastic acquisitions like RandStraddle draw a fresh band width; deterministic ones return a. The two-argument form resample(a, rng) injects an explicit RNG (primary method); the single-argument form resample(a) uses the RNG stored in a (backward-compatible delegate).\n\n\n\n\n\n","title":"Magpie.resample"},{"category":"type","location":"api/#Magpie.AcquisitionDomain","page":"API Reference","text":"AcquisitionDomain\n\nSupertype for the set an acquisition is maximized over: a continuous Box (needs a search strategy) or an explicit Points set (argmax over enumerated inputs).\n\n\n\n\n\n","title":"Magpie.AcquisitionDomain"},{"category":"type","location":"api/#Magpie.Box","page":"API Reference","text":"Box(lb, ub) <: AcquisitionDomain\n\nAxis-aligned box domain with lower and upper bounds lb, ub (per-dimension vectors).\n\n\n\n\n\n","title":"Magpie.Box"},{"category":"type","location":"api/#Magpie.Points","page":"API Reference","text":"Points(X) <: AcquisitionDomain\n\nFinite candidate set: the acquisition is evaluated at each input in X and the best is returned. X may be any point container (Vector{Vector}, ColVecs, …).\n\n\n\n\n\n","title":"Magpie.Points"},{"category":"type","location":"api/#Magpie.AcqMaximizer","page":"API Reference","text":"AcqMaximizer\n\nSupertype for strategies that maximize an acquisition over a continuous Box.\n\n\n\n\n\n","title":"Magpie.AcqMaximizer"},{"category":"type","location":"api/#Magpie.SobolPolish","page":"API Reference","text":"SobolPolish{A} <: AcqMaximizer\n\nTwo-stage maximizer for a Box: score n_candidates low-discrepancy Sobol points, then locally polish the best n_restarts of them with box-constrained LBFGS.\n\nConstructor\n\nSobolPolish(; n_candidates=2048, n_restarts=8, ad=AutoForwardDiff())\n\nad selects the DifferentiationInterface backend for the local polish gradients.\n\n\n\n\n\n","title":"Magpie.SobolPolish"},{"category":"function","location":"api/#Magpie.grid_points","page":"API Reference","text":"grid_points(box::Box; per_axis=50) -> Vector\n\nEnumerate a regular grid over box with per_axis points along each dimension.\n\n\n\n\n\n","title":"Magpie.grid_points"},{"category":"function","location":"api/#Magpie.acquire","page":"API Reference","text":"acquire(al::ActiveLearner; over, maximizer=default_for(over), q=1)\n\nPick the next query point by maximizing the (resampled) acquisition over over. Batch acquisition (q > 1) is not yet implemented.\n\n\n\n\n\n","title":"Magpie.acquire"},{"category":"function","location":"api/#Magpie.saddle_walk","page":"API Reference","text":"saddle_walk(g::ExactGP, x0; box, iters=60, η=0.05, tol=1e-7) -> (x, μ∇, H)\n\nMin-mode (dimer-like) walk to an index-1 saddle of the GP posterior-mean field, starting from x0. At each step the effective force ascends the lowest-curvature mode while descending all others: with v the eigenvector of the smallest eigenvalue of the mean Hessian H̄,\n\nF = -\nablamu + 2(\nablamu cdot v)v qquad x leftarrow mathrmclamp(x + eta F mathrmbox)\n\nIterates until ‖∇μ‖ < tol or iters is reached. Unlike a Newton step, the reflected force is robust on curved reaction paths where Newton is captured by a neighbouring minimum — it is the recommended default for transition_state. Returns the located point, its mean gradient ∇μ, and mean Hessian H̄ (classify with classify).\n\n\n\n\n\n","title":"Magpie.saddle_walk"},{"category":"function","location":"api/#Magpie.newton_polish","page":"API Reference","text":"newton_polish(g::ExactGP, x0; box, iters=20, λ=1e-6, tol=1e-8) -> (x, μ∇, H)\n\nDamped Newton iteration on the posterior-mean field of g, seeking a critical point (∇μ = 0) from x0. Each step solves (H̄ + λI) Δ = ∇μ and clamps the update to box. Returns the located point, its mean gradient, and mean Hessian. This converges to whatever critical point is nearest the basin of x0 (min, saddle, or max) — for an index-1 saddle between two minima, seed it from their midpoint, or use saddle_walk when the connecting path is curved and Newton is captured by a neighbouring extremum.\n\n\n\n\n\n","title":"Magpie.newton_polish"},{"category":"function","location":"api/#Magpie.transition_state","page":"API Reference","text":"transition_state(f, m1, m2; kernel, noise=1e-3, box, budget=12, nseed=5,\n                 predictor=:minmode, η=0.05) -> (; saddle, kind, g, history)\n\nTargeted search for the index-1 transition state (saddle) of f lying between two known minima m1, m2, within a small budget of true f-evaluations.\n\nSeeds an ExactGP (with the given kernel/noise) at m1, m2, and nseed points jittered along the m1→m2 segment, then iterates: predict the saddle on the GP mean (via saddle_walk from the segment midpoint for predictor=:minmode, or newton_polish for predictor=:newton), evaluate the true f there, condition the GP on that point, and repeat until budget total f-evaluations have been spent. The lengthscale is not refit (deliberate: at the scarce n this loop runs in, MLE drives ℓ up and washes out the saddle — pass a fixed feature-scale kernel).\n\nReturns a named tuple: saddle (final predicted location), kind (its Morse type via classify; an index-1 saddle is :saddle), g (the fitted GP), and history (a vector of (neval, predicted_saddle, kind) after each acquisition, for diagnostics).\n\n\n\n\n\n","title":"Magpie.transition_state"},{"category":"function","location":"api/#Magpie.classify","page":"API Reference","text":"classify(H; ε=1e-3) -> Symbol\n\nMorse classification of a critical point from its Hessian H (e.g. the H̄ returned by grad_predict). Returns :min (no negative eigenvalues), :saddle (an index-1 saddle: exactly one negative eigenvalue), :max (all negative), or :unclassified when any eigenvalue is within ε of zero (degenerate / ill-determined curvature).\n\nOnly index-1 saddles map to :saddle; in d≥3, higher-index saddles (≥2 negative eigenvalues but not all) fall to :unclassified — adequate for the 2-D use here.\n\n\n\n\n\n","title":"Magpie.classify"},{"category":"type","location":"api/#Magpie.ActiveLearner","page":"API Reference","text":"ActiveLearner{TX, TY}\n\nMutable state for an active-learning run: the current GP, the acquisition, and the accumulated query history.\n\nFields\n\ngp: the current AbstractGPModel, replaced on each observation\nacq: the acquisition function (reassignable, e.g. to wrap it with LocalPenalization over the live history)\nXs, Ys: accumulated query inputs and observed values, in query order\nacq_vals: the acquisition score at each queried point\nrng: the random-number generator threaded through acquire/run!\n\nConstructor\n\nActiveLearner(gp, acq; rng=Random.default_rng())\n\nStart a learner from an (optionally unconditioned) GP and an acquisition. Both gp and acq are abstractly typed so they can be replaced in place during a run. TX and TY are inferred from gp (inputs are Vector{Float64}; values are Float64 for ExactGP and Bool for LaplaceGP).\n\n\n\n\n\n","title":"Magpie.ActiveLearner"},{"category":"function","location":"api/#Magpie.observe!","page":"API Reference","text":"observe!(al::ActiveLearner, X, Y) -> al\n\nRecord observation(s) (X, Y) and condition the GP on them. Accepts either a single input/value or a batch (a vector of inputs with a matching vector of values).\n\n\n\n\n\n","title":"Magpie.observe!"},{"category":"function","location":"api/#Magpie.fit!","page":"API Reference","text":"fit!(al::ActiveLearner; restarts=1) -> al\n\nRefit the GP's hyperparameters in place (see fit).\n\n\n\n\n\n","title":"Magpie.fit!"},{"category":"function","location":"api/#Magpie.run!","page":"API Reference","text":"run!(al::ActiveLearner, oracle; budget, over, stop=al->false, refit_every=0) -> al\n\nRun the active-learning loop for up to budget rounds. Each round resamples the acquisition, picks the next point over over, queries oracle for its value, and conditions the GP on it. Stops early when stop(al) is true; refits hyperparameters every refit_every rounds (0 disables refitting).\n\n\n\n\n\n","title":"Magpie.run!"},{"category":"function","location":"api/#Magpie.posterior_gp","page":"API Reference","text":"The current posterior GP held by the learner.\n\n\n\n\n\n","title":"Magpie.posterior_gp"},{"category":"function","location":"api/#Magpie.queried_points","page":"API Reference","text":"Copy of the inputs queried so far, in order.\n\n\n\n\n\n","title":"Magpie.queried_points"},{"category":"function","location":"api/#Magpie.all_data","page":"API Reference","text":"Copies of all queried inputs and their observed values, as (Xs, Ys).\n\n\n\n\n\n","title":"Magpie.all_data"},{"category":"section","location":"generated/gp_ude_identifiability/#GP-UDE:-identifiability-(logℓ–logσ-ridge-off-data-divergence)","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Non-identifiability is a feature, not a bug — but it must be surfaced.\n\nA GP-UDE field has a classic flat direction in the (logℓ, logσ) plane: many combinations of lengthscale and output scale, together with the weight vector w, produce nearly identical training-trajectory fits. This example demonstrates:\n\nRidge slice: sweep (logℓ, logσ) holding w fixed at the optimum — reveals the exact scale-invariance and the narrow well in logℓ. The combined landscape is flat (ratio ≫ 1 across vs along the valley).\nMulti-seed divergence: training from different initial (logℓ₀, logσ₀) seeds converges to solutions with similar trajectory RMSE but diverging field predictions off the training support — the identifiability problem in concrete numbers.\n\nDecision D2 (\"surface, don't hide\"): show the problem honestly.\n\nENV[\"GKSwstype\"] = \"100\"  ## GR headless\nusing Magpie, OrdinaryDiffEq, SciMLSensitivity, KernelFunctions, LinearAlgebra, Random\nusing Statistics\nusing Plots; gr()","title":"GP-UDE: identifiability (logℓ–logσ ridge + off-data divergence)"},{"category":"section","location":"generated/gp_ude_identifiability/#True-system-(Lotka-Volterra)","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"function lv!(du, u, p, t)\n    α, β, γ, δ = 1.5, 1.0, 3.0, 1.0\n    du[1] = α * u[1] - β * u[1] * u[2]\n    du[2] = δ * u[1] * u[2] - γ * u[2]\n    return nothing\nend\n\nlv_true(u) = [1.5 * u[1] - u[1] * u[2], u[1] * u[2] - 3.0 * u[2]]\n\nu0 = [1.0, 1.0]\ntspan = (0.0, 3.0)\nts = collect(range(tspan...; length = 15))\n\ntarget = Array(solve(ODEProblem(lv!, u0, tspan), Tsit5(); saveat = ts))\n\nσ_obs_true = 0.03f0\nrng_noise = MersenneTwister(123)\nXnoisy = target .+ σ_obs_true .* randn(rng_noise, size(target))\n\nShared anchor grid (same Z for all seeds so w-blocks correspond to same locations).\n\nRandom.seed!(42)\nZ = kmeans_anchors(Xnoisy, 12; rng = MersenneTwister(7))\n\nExtension (get once — field_loss lives in MagpieSciMLExt).\n\next = Base.get_extension(Magpie, :MagpieSciMLExt)","title":"True system (Lotka-Volterra)"},{"category":"section","location":"generated/gp_ude_identifiability/#1.-Ridge-slice:-sweep-(logℓ,-logσ)-with-w-fixed-at-the-optimum","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Train once (weak λ=1e-4 logℓ prior so it lands freely on the ridge) to get a reference optimum, then sweep (logℓ, logσ) while holding w fixed. This reveals the exact scale-symmetry: predmean = kuZ'α is INVARIANT to logσ when w is fixed (logσ enters both kuZ and K_ZZ symmetrically; the scale cancels in α). Result: a flat valley in logσ at every logℓ, and a sharp well in logℓ.\n\nRandom.seed!(1)\nfield_ref = ExactGPField(SqExponentialKernel(), Z; d = 2, logℓ0 = 0.0)\ntrain!(field_ref, (ts, Xnoisy); tspan, maxiters = 150, λ = 1.0e-4, s = 0.5)\nvopt = field_ref.v0\n\n@info \"Reference optimum\" logℓ = round(vopt[1]; digits = 3) logσ = round(vopt[2]; digits = 3)\n\nPure data-fit loss (no regularizer) for the ridge slice.\n\nloss_raw = ext.field_loss(\n    field_ref, Magpie.SingleShooting(), [(ts, Xnoisy)];\n    tspan, u0 = u0, λ = 0.0, λσ = 0.0\n)\nloss_at_opt = loss_raw(vopt)\n@info \"Data-fit loss at reference optimum\" loss_at_opt\n\nlogℓ_ref, logσ_ref = vopt[1], vopt[2]\nxs = range(logℓ_ref - 1.2, logℓ_ref + 1.2; length = 11)  # logℓ axis\nys = range(logσ_ref - 1.5, logσ_ref + 1.5; length = 11)  # logσ axis\n\n@info \"Computing 11×11 ridge slice (121 forward solves, w fixed at reference optimum)...\"\nM_slice = ridge_slice(loss_raw, vopt; idx = (1, 2), grid = (xs, ys))\n\nRidge flatness: full-grid range vs valley-internal range. \"Valley\" = cells with data-fit loss < 3× the minimum value in the slice.\n\nloss_vals_finite = filter(v -> isfinite(v) && v < 1.0e5, vec(M_slice))\nloss_min = minimum(loss_vals_finite)\nloss_range_full = maximum(loss_vals_finite) - loss_min\nvalley_vals = filter(v -> v < loss_min * 3.0, loss_vals_finite)\nloss_range_valley = isempty(valley_vals) ? 0.0 : maximum(valley_vals) - minimum(valley_vals)\nflatness_ratio = loss_range_full / max(loss_range_valley, 1.0e-10)\n\n@info \"Ridge flatness (w fixed at optimum)\" valley_range = round(loss_range_valley; digits = 3) full_range = round(loss_range_full; digits = 3) ratio = round(flatness_ratio; digits = 0)\n\nHeatmap (clamp diverged solves to a finite ceiling).\n\nM_plot = clamp.(M_slice, 0.0, loss_at_opt * 8)\np_ridge = heatmap(\n    collect(xs), collect(ys), M_plot';\n    xlabel = \"logℓ\", ylabel = \"logσ\",\n    title = \"Data-fit landscape: (logℓ, logσ) with w fixed at optimum\",\n    color = :viridis, clims = (minimum(M_plot), loss_at_opt * 5)\n)\nscatter!(p_ridge, [logℓ_ref], [logσ_ref]; marker = :star5, ms = 10, mc = :red, label = \"ref optimum\")\nsavefig(p_ridge, \"identifiability_ridge.png\")","title":"1. Ridge slice: sweep (logℓ, logσ) with w fixed at the optimum"},{"category":"section","location":"generated/gp_ude_identifiability/#2.-Multi-seed-training:-converge-to-different-ridge-solutions","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Three seeds with different logℓ₀ initializations converge to different (logℓ, logσ) combinations.  With a very weak logℓ prior (λ=1e-4), the optimizer can land anywhere on the flat logσ direction and in a modest well in logℓ. We report trajectory RMSE (vs clean truth) as the \"data fit\" metric — this is fairer than NLL because the observation-noise σ_obs is also trained and can drive NLL very negative while RMSE stays bounded.\n\nSEED_INITS = [(1, 0.0), (10, 1.5), (17, 0.3)]   # (seed, logℓ₀)\n\n_train_seed(seed, logℓ0) = begin\n    Random.seed!(seed)\n    f = ExactGPField(SqExponentialKernel(), Z; d = 2, logℓ0 = logℓ0)\n    train!(f, (ts, Xnoisy); tspan, maxiters = 150, λ = 1.0e-4, s = 0.5)\n\nTrajectory RMSE: integrate GP mean field vs clean truth (same metric as the LV example).\n\n    g = posterior(f)\n    gp_rhs!(du, u, p, t) = (du .= vec(mean(g, [u])); nothing)\n    sol_gp = Array(solve(ODEProblem(gp_rhs!, u0, tspan), Tsit5(); saveat = ts))\n    traj_rmse = sqrt(sum(abs2, sol_gp .- target) / length(target))\n    (v = f.v0, gps = g, traj_rmse = traj_rmse)\nend\n\n@info \"Training 3 seeds (weak regulariser)...\"\nseed_results = [_train_seed(seed, logℓ0) for (seed, logℓ0) in SEED_INITS]\nvopts_ms = [r.v     for r in seed_results]\ngps_ms = [r.gps   for r in seed_results]\ntraj_rmses = [r.traj_rmse for r in seed_results]\n\n@info \"Multi-seed trajectory RMSE (all should be similar — fitted the data):\"\nfor ((seed, logℓ0), v, rmse) in zip(SEED_INITS, vopts_ms, traj_rmses)\n    @info \"  Seed $seed (logℓ₀=$logℓ0)\" logℓ = round(v[1]; digits = 3) logσ = round(v[2]; digits = 3) traj_rmse = round(rmse; digits = 4)\nend","title":"2. Multi-seed training: converge to different ridge solutions"},{"category":"section","location":"generated/gp_ude_identifiability/#3.-Off-data-divergence","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Measure field error at off-manifold test points for each seed solution. These points are OUTSIDE the training trajectory support. Even though all seeds fit the training trajectory (similar RMSE), their field predictions diverge off the training support.\n\nprey_grid = range(0.3, 2.5; length = 10)\npred_grid = range(0.3, 3.5; length = 10)\noffpts = vec([[p1, p2] for p1 in prey_grid, p2 in pred_grid])\n\nferrs_ms = map(enumerate(zip(SEED_INITS, gps_ms))) do (i, ((seed, logℓ0), gps))\n    fe = field_error(gps, lv_true, offpts)\n    @info \"  Off-data field error\" seed logℓ = round(vopts_ms[i][1]; digits = 3) median = round(fe.median; digits = 4) q90 = round(fe.q90; digits = 4)\n    fe\nend\n\nall_medians_ms = [fe.median for fe in ferrs_ms]\nferr_spread_ms = maximum(all_medians_ms) - minimum(all_medians_ms)\ntraj_rmse_spread = maximum(traj_rmses) - minimum(traj_rmses)\n\n@info \"Summary\" traj_rmse_spread = round(traj_rmse_spread; digits = 4) ferr_spread = round(ferr_spread_ms; digits = 4) medians = round.(all_medians_ms; digits = 4)","title":"3. Off-data divergence"},{"category":"section","location":"generated/gp_ude_identifiability/#4.-Visualise-the-divergence","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Two side-by-side bars: trajectory RMSE (should be similar) and off-data field error (should diverge) for each seed solution.\n\nseed_labels = [\n    \"seed $(s)\\nlogℓ=$(round(v[1]; digits = 2))\"\n        for ((s, li), v) in zip(SEED_INITS, vopts_ms)\n]\n\np_rmse = bar(\n    seed_labels, traj_rmses;\n    ylabel = \"Trajectory RMSE\",\n    title = \"On-data fit (similar — all fit the trajectory)\",\n    legend = false, color = :steelblue\n)\n\np_ferr = bar(\n    seed_labels, all_medians_ms;\n    ylabel = \"Field error (off-data median)\",\n    title = \"Off-data divergence (similar on-data, diverging off-data)\",\n    legend = false, color = :orange\n)\n\np_both = plot(p_rmse, p_ferr; layout = (1, 2), size = (800, 350))\nsavefig(p_both, \"identifiability_divergence.png\")","title":"4. Visualise the divergence"},{"category":"section","location":"generated/gp_ude_identifiability/#Anti-rot-assertions-(#src-lines-run-on-direct-execution-only)","page":"GP-UDE: identifiability (ridge + off-data divergence)","text":"Ridge flatness: the raw data-fit loss (w fixed) spans much more variation across the full (logℓ, logσ) grid than within the low-loss valley. A ratio > 5 is conservative; actual is typically > 1e6 (exact scale symmetry).\nTrajectory RMSE spread across seeds: all solutions fit the training trajectory reasonably well. A spread < 0.15 confirms all seeds found valid fits.\nOff-data field error spread: different ridge solutions diverge off the training manifold. A spread > 0.1 confirms the identifiability problem.\n\nGP-UDE training is multi-basin / BLAS-sensitive, so the per-seed fit QUALITY (trajrmse, the off-data spread magnitude) varies with the backend — the robust gate is `flatnessratio` above, a property of the loss LANDSCAPE (computed with weights fixed), which is training-independent and\n\n\n\nThis page was generated using Literate.jl.","title":"Anti-rot assertions (#src lines run on direct execution only)"},{"category":"section","location":"generated/muller_brown/#Müller–Brown:-a-transition-state-from-one-known-minimum","page":"Müller–Brown: a transition state from one known minimum","text":"The Müller–Brown potential (Müller & Brown, Theor. Chim. Acta 53, 75 (1979)) is the standard 2-D benchmark for transition-state search: a sum of four Gaussians with three minima (metastable states) joined by two index-1 saddles (the transition states on the minimum-energy path between basins).\n\nIn chemistry, locating a transition state is expensive because each energy evaluation is a DFT calculation. We can model this with a GP: treat the potential as a black-box f : ℝ² → ℝ, start from one known stable state (MC_min, the deep right basin), and find the transition state leading out of it by climbing the GP surrogate's softest mode — evaluating the true f only when the walk predicts a new saddle candidate. This is the single-ended (dimer-on-GP) workflow used in Denzel & Kästner (2018), here on the canonical toy surface.","title":"Müller–Brown: a transition state from one known minimum"},{"category":"section","location":"generated/muller_brown/#Setup","page":"Müller–Brown: a transition state from one known minimum","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, LinearAlgebra, Random\nusing Statistics: mean, std\nusing Plots; gr()\nimport KernelFunctions as KF","title":"Setup"},{"category":"section","location":"generated/muller_brown/#The-potential-and-its-landscape","page":"Müller–Brown: a transition state from one known minimum","text":"The four Gaussians give a deep right basin, a shallow central basin, and a high-left basin, separated by two saddles. We work on the conventional window and clip the steep exponential walls for display (the true f reaches ~10³ at the corners).\n\nconst A = (-200.0, -100.0, -170.0, 15.0)\nconst aa = (-1.0, -1.0, -6.5, 0.7);  const bb = (0.0, 0.0, 11.0, 0.6)\nconst cc = (-10.0, -10.0, -6.5, 0.7)\nconst x0 = (1.0, 0.0, -0.5, -1.0);   const y0 = (0.0, 0.5, 1.5, 1.0)\n\nfunction mullerbrown(p)\n    x, y = p[1], p[2]; s = 0.0\n    for k in 1:4\n        dx = x - x0[k]; dy = y - y0[k]\n        s += A[k] * exp(aa[k] * dx^2 + bb[k] * dx * dy + cc[k] * dy^2)\n    end\n    return s\nend\n\nbox = Box([-1.5, -0.5], [1.0, 2.0])\n\nThe five true critical points (literature values): three minima and two index-1 saddles. MC_min is the deep right basin — the one we start from. S2 is the transition state leading out of it toward the central basin MB_min.\n\nconst MA_min = [-0.558, 1.442]    # high-left minimum\nconst MB_min = [-0.05, 0.467]     # shallow central minimum\nconst MC_min = [0.623, 0.028]     # deep right minimum (starting point)\nconst S1 = [-0.822, 0.624]        # saddle between MA and MB\nconst S2 = [0.212, 0.293]         # saddle between MB and MC  (escape TS from MC_min)\n\ntruth = [MA_min, MB_min, MC_min, S1, S2]\ntrue_kinds = [:min, :min, :min, :saddle, :saddle]\n\nxl = range(-1.5, 1.0; length = 100); yl = range(-0.5, 2.0; length = 100)\nVclip(p) = min(mullerbrown(p), 150.0)                 ## clip walls for a readable contour\nZf = [Vclip([xi, yj]) for yj in yl, xi in xl]\n\nThe landscape: minima (circles) and saddles (diamonds) are the five critical points. We start from MC_min — the deep right basin — and ask: where is the transition state leading out of it?\n\nplt_contour = contourf(\n    xl, yl, Zf; levels = 20, c = :viridis, colorbar_title = \"V (clipped)\",\n    aspect_ratio = :equal, xlims = (-1.5, 1.0), ylims = (-0.5, 2.0),\n    xlabel = \"x\", ylabel = \"y\", title = \"Müller–Brown potential\"\n)\nfor (kind, mk, col, lab) in ((:min, :circle, :white, \"minima\"), (:saddle, :diamond, :orange, \"saddles (TS)\"))\n    idx = findall(==(kind), true_kinds)\n    scatter!(\n        plt_contour, [truth[i][1] for i in idx], [truth[i][2] for i in idx];\n        m = mk, ms = 8, mc = col, msw = 1.2, label = lab\n    )\nend\nscatter!(\n    plt_contour, [MC_min[1]], [MC_min[2]];\n    m = :star5, ms = 10, mc = :cyan, msw = 1.0, label = \"MC_min (start)\"\n)\nplt_surface = surface(\n    xl, yl, Zf; c = :viridis, colorbar = false, camera = (35, 45),\n    xlabel = \"x\", ylabel = \"y\", zlabel = \"V\", title = \"the basins and barriers\"\n)\nplot(plt_contour, plt_surface; layout = (1, 2), size = (960, 410))\n\nThe minima are metastable chemical states; the saddles are the transition states that gate the rate of hopping between them. S2 is the escape saddle from MC_min.","title":"The potential and its landscape"},{"category":"section","location":"generated/muller_brown/#Taming-the-dynamic-range","page":"Müller–Brown: a transition state from one known minimum","text":"The raw potential spans ~10³ over the box (deep wells, exp walls). We clip at a ceiling and z-score so the surface the GP sees is ~unit-scale — the standard surrogate-conditioning move for stiff potentials. We fix the lengthscale rather than refitting: at the scarce sample sizes here, marginal-likelihood fitting drives ℓ up and washes the wells out.\n\nconst CEIL = 200.0\nlet g = grid_points(box; per_axis = 50), v = min.(mullerbrown.(g), CEIL)\n    global const _μ = mean(v); global const _σ = std(v)\nend\nmbt(p) = (min(mullerbrown(p), CEIL) - _μ) / _σ        ## clipped, z-scored — what we condition on\n\nconst NOISE = 1.0e-3","title":"Taming the dynamic range"},{"category":"section","location":"generated/muller_brown/#A-custom-anisotropic-Matérn-kernel","page":"Müller–Brown: a transition state from one known minimum","text":"The potential is rough and the reaction valley is narrow across the path but flat along it, so an anisotropic Matérn is the honest prior. Plain MaternKernel is non-differentiable at r=0, which breaks the gradient-variance term grad_predict needs (the squared distance hides a √); we sidestep it by evaluating the kernel in r² with a short Taylor branch near 0 (technique from CovarianceFunctions.jl, MIT-licensed). One lengthscale per input axis gives the anisotropy.\n\nstruct TaylorMatern32{V} <: KF.Kernel\n    invℓ::V\nend\nfunction (k::TaylorMatern32)(x, y)\n    s = sum(abs2, (x .- y) .* k.invℓ)                       # r² = Σ ((xᵢ-yᵢ)/ℓᵢ)²\n    if s < 1.0e-6\n        return 1 - 1.5 * s - 1.125 * s^2                    # Taylor of ψ(r²) near 0 (AD-smooth)\n    else\n        r = sqrt(s)\n        return (1 + sqrt(3) * r) * exp(-sqrt(3) * r)\n    end\nend\n\nmbkernel() = TaylorMatern32(1.0 ./ [0.35, 0.45])            # anisotropic feature scales (x, y)","title":"A custom anisotropic Matérn kernel"},{"category":"section","location":"generated/muller_brown/#Finding-the-escape-transition-state-from-one-known-minimum","page":"Müller–Brown: a transition state from one known minimum","text":"We know one stable state (MC_min, the deep right basin) and nothing else. To find the transition state leading out of it we climb the softest local mode (a gentlest-ascent / dimer walk) on the GP mean, evaluate the true potential where the walk predicts the saddle plus one GradStraddle point to reduce gradient uncertainty along the way, re-condition, and repeat. The GP surrogate replaces the expensive inner force evaluations a classical dimer would need.\n\nEach iteration: (1) compute the softest Hessian eigenvector at MC_min (the escape direction); (2) run saddle_walk in both signs of that direction and keep any index-1 result; (3) evaluate mbt at the predicted saddle and one GradStraddle point; (4) update the GP.\n\nfunction escape_saddle(f, m0, kernel, box; budget = 14, noise = NOISE, rng = Random.default_rng())\n    pts = [clamp.(m0 .+ 0.08 .* randn(rng, 2), box.lb, box.ub) for _ in 1:5]\n    push!(pts, collect(float.(m0)))\n    g = update(ExactGP(kernel; noise = noise), pts, f.(pts))\n    hist = Tuple{Int, Vector{Float64}, Symbol}[]\n    res = (; x = collect(float.(m0)), H = zeros(2, 2))\n    for t in 1:budget\n        v = eigen(Symmetric(grad_predict(g, m0)[3])).vectors[:, 1]    # softest mode at the basin\n        # climb both signs on the surrogate; keep the branch that reaches an index-1 saddle\n        walks = [saddle_walk(g, clamp.(m0 .+ 0.2 .* s .* v, box.lb, box.ub); box = box) for s in (1.0, -1.0)]\n        saddles = filter(w -> classify(w.H) == :saddle, walks)\n        res = isempty(saddles) ? argmin(w -> w.residual, walks) : argmin(w -> w.residual, saddles)\n        xacq = acquire(g, GradStraddle(β = 1.96); over = box)         ## explore high gradient-uncertainty\n        g = update(g, [res.x, xacq], f.([res.x, xacq]))\n        push!(hist, (length(pts) + 2t, copy(res.x), classify(res.H)))\n    end\n    return res, g, hist\nend\n\nRandom.seed!(1)\nres, gfit, hist = escape_saddle(mbt, MC_min, mbkernel(), box; budget = 14)\n@info \"escape transition state\" predicted = round.(res.x; digits = 4) truth_S2 = S2 err = round(norm(res.x .- S2); digits = 4) kind = classify(res.H)","title":"Finding the escape transition state from one known minimum"},{"category":"section","location":"generated/muller_brown/#Landscape-and-the-climb","page":"Müller–Brown: a transition state from one known minimum","text":"The contour below shows the true potential with the sequence of saddle candidates the loop predicted at each iteration. Early iterations are scattered near MC_min; they converge toward S2 as the surrogate fills in the reaction valley.\n\nxc = range(-1.5, 1.0; length = 80); yc = range(-0.5, 2.0; length = 80)\nZc = [Vclip([xi, yj]) for yj in yc, xi in xc]\nplt_climb = contourf(\n    xc, yc, Zc; levels = 18, c = :viridis, colorbar = false,\n    aspect_ratio = :equal, xlims = (-1.5, 1.0), ylims = (-0.5, 2.0),\n    xlabel = \"x\", ylabel = \"y\", title = \"saddle predictions (loop history)\",\n    size = (520, 460)\n)\npred_xs = [h[2][1] for h in hist]; pred_ys = [h[2][2] for h in hist]\nscatter!(plt_climb, pred_xs, pred_ys; ms = 5, mc = :white, msw = 0.5, label = \"predicted saddle\")\nscatter!(plt_climb, [MC_min[1]], [MC_min[2]]; m = :star5, ms = 9, mc = :cyan, msw = 0.8, label = \"MC_min (start)\")\nscatter!(plt_climb, [S2[1]], [S2[2]]; m = :star5, ms = 9, mc = :gold, msw = 0.8, label = \"true S2\")\nscatter!(plt_climb, [res.x[1]], [res.x[2]]; m = :star5, ms = 10, mc = :red, msw = 0.8, label = \"final prediction\")","title":"Landscape and the climb"},{"category":"section","location":"generated/muller_brown/#Surrogate-at-the-end-of-the-loop","page":"Müller–Brown: a transition state from one known minimum","text":"After the loop the GP posterior mean already resolves the reaction valley from MC_min to S2. The evaluation sites (circles) trace the path the loop explored.\n\nZm = [predmean(gfit, [xi, yj]) for yj in yc, xi in xc]\nplt_surr = contourf(\n    xc, yc, Zm; levels = 18, c = :viridis, colorbar = false,\n    aspect_ratio = :equal, xlims = (-1.5, 1.0), ylims = (-0.5, 2.0),\n    xlabel = \"x\", ylabel = \"y\", title = \"GP posterior mean (end of loop)\",\n    size = (520, 460)\n)\nscatter!(plt_surr, [S2[1]], [S2[2]]; m = :star5, ms = 9, mc = :gold, msw = 0.8, label = \"true S2\")\nscatter!(plt_surr, [res.x[1]], [res.x[2]]; m = :star5, ms = 10, mc = :red, msw = 0.8, label = \"predicted saddle\")\n\nplot(plt_climb, plt_surr; layout = (1, 2), size = (1040, 460))","title":"Surrogate at the end of the loop"},{"category":"section","location":"generated/muller_brown/#Correctness:-the-Hessian-diagnostic","page":"Müller–Brown: a transition state from one known minimum","text":"A first-order transition state has Morse index 1: one negative eigenvalue (the reaction coordinate) and one positive (the perpendicular stiffness). We confirm this at the located point via grad_predict.\n\nλ = eigen(Symmetric(grad_predict(gfit, res.x)[3])).values\n@info \"Hessian eigenvalues at the located saddle\" λ = round.(λ; digits = 3) index = count(<(0), λ)\n\n@assert classify(grad_predict(gfit, res.x)[3]) == :saddle                               #src located point is an index-1 saddle","title":"Correctness: the Hessian diagnostic"},{"category":"section","location":"generated/muller_brown/#Convergence-vs-a-random-baseline","page":"Müller–Brown: a transition state from one known minimum","text":"Localization error ‖predicted − S2‖ vs evaluation count from the loop's history, against a random-sampling baseline (uniform points in box, GP built with the same kernel, nearest index-1 saddle of the posterior mean to S2).\n\nfunction random_nearest_saddle(f, kernel, box; T = 34, noise = NOISE, rng = Random.default_rng())\n    pts = [box.lb .+ (box.ub .- box.lb) .* rand(rng, 2) for _ in 1:T]\n    g = update(ExactGP(kernel; noise = noise), pts, f.(pts))\n    pol = [newton_polish(g, x; box = box, iters = 20) for x in grid_points(box; per_axis = 25)]\n    conv = filter(p -> p.residual < 1.0e-2, pol)\n    uniq = unique(p -> round.(p.x; digits = 1), conv)\n    sad = [p.x for p in uniq if classify(p.H) == :saddle]\n    return isempty(sad) ? Inf : minimum(norm(s .- S2) for s in sad)\nend\n\nns = [h[1] for h in hist]\nerrs = [norm(h[2] .- S2) for h in hist]\n\nRandom.seed!(42)\nrand_err = random_nearest_saddle(mbt, mbkernel(), box; T = ns[end])\n@info \"convergence comparison\" loop_final_err = round(errs[end]; digits = 4) random_err = round(rand_err; digits = 4)\n\nplt_conv = plot(\n    ns, errs; lw = 2, marker = :circle, mc = :steelblue, lc = :steelblue,\n    label = \"escape_saddle loop\",\n    xlabel = \"true f-evaluations\", ylabel = \"‖predicted − S2‖\",\n    title = \"Transition-state localization error\", size = (560, 380), legend = :topright\n)\nhline!(plt_conv, [rand_err]; lc = :darkorange, ls = :dash, lw = 2, label = \"random (nearest saddle, same budget)\")\nhline!(plt_conv, [0.1]; lc = :gray, ls = :dot, lw = 1, label = \"0.1 threshold\")","title":"Convergence vs a random baseline"},{"category":"section","location":"generated/muller_brown/#Takeaway","page":"Müller–Brown: a transition state from one known minimum","text":"The single-ended loop — climb the softest GP mode from MC_min, evaluate the true potential at the predicted saddle and a gradient-uncertainty point, repeat — locates S2 in a few dozen true f-evaluations, well below the 0.1 localization threshold. The random baseline, with no notion of which saddle it is after, does not reliably find it at the same budget. The Hessian diagnostic (one negative eigenvalue) confirms the located point is a genuine first-order transition state.\n\nThe machinery is kernel-generic: swapping TaylorMatern32 for any other kernel, or pointing mbt at a real DFT driver, gives the same loop on a real PES.\n\n\n\nThis page was generated using Literate.jl.","title":"Takeaway"},{"category":"section","location":"generated/volcano_terrain/#Volcano-terrain:-Morse-critical-points-of-a-real-elevation-model","page":"Volcano terrain: Morse critical points of a DEM","text":"Critical-point extraction is not just for toy potentials. A digital elevation model (DEM) is a measured scalar field h(x, y), and its Morse critical points are literal landforms: summits are maxima, basins / crater floors are minima, and mountain passes (cols) are saddles. Geostatistical interpolation of terrain — kriging — is Gaussian-process regression, so a DEM is a natural home for the same derivative-GP machinery the Müller–Brown example used on a chemical potential.\n\nHere we take the classic Maunga Whau (Mt Eden) volcano DEM — the public-domain R volcano dataset, an 87×61 grid on a 10 m lattice — and, from elevation samples, recover and Morse-classify its critical points: the summit ring, the crater, and the passes between them.","title":"Volcano terrain: Morse critical points of a real elevation model"},{"category":"section","location":"generated/volcano_terrain/#Setup","page":"Volcano terrain: Morse critical points of a DEM","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, LinearAlgebra\nusing DelimitedFiles: readdlm\nusing Statistics: mean, std\nusing Plots; gr()","title":"Setup"},{"category":"section","location":"generated/volcano_terrain/#The-terrain","page":"Volcano terrain: Morse critical points of a DEM","text":"Load the DEM (bundled as a CSV of integer metres; no dataset dependency) and z-score the elevations so a unit-variance kernel is well-scaled. We work in normalized [0,1]² map coordinates and interpolate the grid bilinearly to evaluate h at arbitrary query points.\n\nraw = readdlm(joinpath(pkgdir(Magpie), \"examples\", \"data\", \"volcano.csv\"), ',', Int)\nnrow, ncol = size(raw)\nZraw = Float64.(raw)\nμz, σz = mean(Zraw), std(Zraw)\nZ = (Zraw .- μz) ./ σz                       ## z-scored elevation field on the grid\n\nfunction bilinear(M, x, y)                    ## sample grid M at normalized (x,y) ∈ [0,1]²\n    nr, nc = size(M)\n    ci = clamp(x * (nc - 1) + 1, 1.0, Float64(nc)); ri = clamp(y * (nr - 1) + 1, 1.0, Float64(nr))\n    c0, r0 = floor(Int, ci), floor(Int, ri); c1, r1 = min(c0 + 1, nc), min(r0 + 1, nr)\n    dc, dr = ci - c0, ri - r0\n    return (1 - dc) * (1 - dr) * M[r0, c0] + dc * (1 - dr) * M[r0, c1] +\n        (1 - dc) * dr * M[r1, c0] + dc * dr * M[r1, c1]\nend\nhz(p) = bilinear(Z, p[1], p[2])               ## z-scored elevation (what the GP fits)\nhm(p) = bilinear(Zraw, p[1], p[2])            ## elevation in metres (for labelling)\n\nxs = range(0, 1; length = ncol); ys = range(0, 1; length = nrow)\nZm = [Zraw[r, c] for r in 1:nrow, c in 1:ncol]\nplt_contour = contourf(\n    xs, ys, Zm; levels = 18, c = :terrain, colorbar_title = \"elevation (m)\",\n    aspect_ratio = :equal, xlims = (0, 1), ylims = (0, 1),\n    xlabel = \"x\", ylabel = \"y\", title = \"Maunga Whau DEM (87×61 @ 10 m)\"\n)\nplt_surface = surface(\n    xs, ys, Zm; c = :terrain, colorbar = false, camera = (40, 55),\n    xlabel = \"x\", ylabel = \"y\", zlabel = \"elevation (m)\", title = \"the cone, crater, and rim\"\n)\nplot(plt_contour, plt_surface; layout = (1, 2), size = (960, 410))\n\nThe cone rises to a summit ring around a central crater; the rim is notched by several passes. Those are the maxima, minimum, and saddles we expect the extractor to find.","title":"The terrain"},{"category":"section","location":"generated/volcano_terrain/#Fit-a-GP-and-extract-the-critical-points","page":"Volcano terrain: Morse critical points of a DEM","text":"Condition an ExactGP on a coverage sample of the DEM (a step-4 subgrid, ~350 points) and fit the lengthscales and signal variance with Magpie.fit. grad_predict then gives the posterior gradient mean μ∇, gradient variance Σ∇, and mean Hessian H̄; multi-start Newton finds the gradient-zeros and classify types them by Morse index. The terrain has a mild east–west vs north–south anisotropy (the cone is slightly elongated), so we use an ARD squared-exponential kernel that fits a separate lengthscale per map direction.\n\nfunction critical_points(g, box; per_axis = 35, ε = 1.0e-3, restol = 5.0e-3, maxvar = 1.2)\n    pol = [newton_polish(g, x; box = box, iters = 20) for x in grid_points(box; per_axis = per_axis)]\n    conv = filter(p -> norm(p[2]) < restol, pol)\n    uniq = unique(p -> round.(p[1]; digits = 2), conv)\n    cps = map(uniq) do (x, _, H)\n        (point = x, kind = classify(H; ε = ε), gvar = maximum(grad_predict(g, x)[2]))\n    end\n    return filter(cps) do c                          ## two filters for finite-DEM artifacts:\n        all(0.06 .< c.point .< 0.94) &&       ##   interior mask — drop boundary edge effects\n            c.gvar ≤ maxvar                    ##   gradient-variance prune — drop under-pinned zeros\n    end\nend\n\nbox = Box([0.0, 0.0], [1.0, 1.0])\nstep = 4\nXs = [[xs[c], ys[r]] for r in 1:step:nrow for c in 1:step:ncol]\nYs = [Z[r, c]        for r in 1:step:nrow for c in 1:step:ncol]\ng = update(ExactGP(with_lengthscale(SqExponentialKernel(), [1 / 6, 1 / 6]); noise = 0.01), Xs, Ys)\ng = Magpie.fit(g; restarts = 5)   # fits per-axis lengthscales for the terrain\n@info \"fitted ARD lengthscales (x, y)\" ℓ = round.(1 ./ g.prior.kernel.kernel.transform.v; digits = 3)\ncps = critical_points(g, box)\n\nnmax = count(c -> c.kind == :max, cps)\nnmin = count(c -> c.kind == :min, cps)\nnsad = count(c -> c.kind == :saddle, cps)\n@info \"recovered terrain critical points\" n_samples = length(Xs) maxima = nmax minima = nmin saddles = nsad\n\nAll three Morse types appear: a ring of maxima on the summit (~180–190 m), the crater-floor and a corner-slope minimum (~96–150 m), and saddles at the passes between rim high points.\n\nxg = range(0, 1; length = 110); yg = range(0, 1; length = 110)\nZgn = [norm(grad_predict(g, [xi, yj])[1]) for yj in yg, xi in xg]\nplt_recovered = contourf(\n    xs, ys, Zm; levels = 18, c = :terrain, colorbar_title = \"elevation (m)\",\n    aspect_ratio = :equal, xlims = (0, 1), ylims = (0, 1),\n    xlabel = \"x\", ylabel = \"y\", title = \"Recovered & classified terrain features\"\n)\nfor (kind, mk, col, lab) in (\n        (:max, :utriangle, :red, \"summit (max)\"),\n        (:min, :circle, :blue, \"basin (min)\"),\n        (:saddle, :diamond, :yellow, \"pass (saddle)\"),\n    )\n    P = [c.point for c in cps if c.kind == kind]\n    isempty(P) || scatter!(\n        plt_recovered, [p[1] for p in P], [p[2] for p in P];\n        m = mk, ms = 7, mc = col, msw = 1.2, label = lab\n    )\nend\nplt_recovered\n\nnote: Two filters for a finite, measured field\nA DEM is sampled on a bounded grid, so multi-start Newton throws off two kinds of false positives: gradient-zeros pinned against the boundary (an artifact of the finite domain), and spurious near-zeros where the field is poorly determined (high posterior gradient variance Σ∇). We drop the first with an interior mask and the second with a Σ∇ threshold — here the genuine interior features sit at Σ∇ ≲ 1, the boundary artifacts well above it.","title":"Fit a GP and extract the critical points"},{"category":"section","location":"generated/volcano_terrain/#Why-no-active-learning-curve-here","page":"Volcano terrain: Morse critical points of a DEM","text":"Unlike a level-set or a targeted transition-state search, this example is extraction on real data, not an active-learning contest — and deliberately so. The volcano's critical points are spatially clustered on and around the summit ring, so concentrating a budget by an acquisition does not beat plain coverage sampling at enumerating them: the Newton extractor needs spread-out samples to represent every basin, and a tight cluster of features is exactly the case where coverage already does well. The value on show is the kernel-generic derivative-GP extraction and Morse classification working on a real measured field — recovering all three landform types — not an acquisition that wins.","title":"Why no active-learning curve here"},{"category":"section","location":"generated/volcano_terrain/#Takeaway","page":"Volcano terrain: Morse critical points of a DEM","text":"The derivative-GP pipeline — grad_predict, multi-start-Newton extraction, and classify Morse typing — is a general, kernel-generic tool that applies unchanged from an analytic chemical potential to a real digital elevation model: feed it samples of any smooth scalar field and it returns the field's summits, basins, and passes, each typed by Morse index. This example maps all the landforms by coverage; the Müller–Brown example takes the complementary, targeted route — a single-ended active search for one specific transition state — so the two together cover the general extraction tool and the targeted active win.\n\n\n\nThis page was generated using Literate.jl.","title":"Takeaway"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#GP-UDE:-Lotka-Volterra","page":"GP-UDE: Lotka-Volterra","text":"Learn the predator–prey vector field from noisy observations.\n\nA single trajectory over (0, 3) with observation noise σ_obs ≈ 0.03 is enough for single-shooting to recover the limit-cycle dynamics. ExactGPField is trained end-to-end via train! (ADAM warm-up → LBFGS polish). Recovery is measured three honest ways:\n\nTrajectory RMSE — ODE integration of the GP posterior mean field vs the clean ground truth.  PULL is the uncertainty propagator, not the mean-trajectory integrator: the Euler recurrence in PULL accumulates much larger errors than a proper ODE solver on the same mean field.\nField error — GP vector field vs true RHS, on-trajectory and off-manifold (grid around the limit cycle).\nHeld-out-IC coverage — propagate uncertainty from a fresh initial condition and check that the clean trajectory falls inside the band. Two propagators are compared:\nPULL — a cheap analytic moment-matching propagator. Its mean uses a first-order Euler recurrence, which drifts off the true trajectory on a nonlinear limit cycle; coverage collapses to ~0. This is a documented Euler limitation of PULL, not a failure of the learned field.\nPathwise — a Monte-Carlo ensemble of decoupled GP samples, each integrated as a proper ODE. Its empirical band achieves nominal-or- conservative coverage of the held-out truth (here it over-covers: coverage = 1.0 at 90% nominal, i.e. the band is wide/conservative rather than tightly calibrated). The gate is a one-sided lower bound: it guards against under-coverage regressions, not against over-dispersion.\n\nNone of these metrics is in-sample loss.\n\nENV[\"GKSwstype\"] = \"100\"  ## GR headless\nusing Magpie, OrdinaryDiffEq, SciMLSensitivity, KernelFunctions, LinearAlgebra, Random\nusing Statistics\nusing Plots; gr()","title":"GP-UDE: Lotka-Volterra"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#True-system","page":"GP-UDE: Lotka-Volterra","text":"function lv!(du, u, p, t)\n    α, β, γ, δ = 1.5, 1.0, 3.0, 1.0\n    du[1] = α * u[1] - β * u[1] * u[2]\n    du[2] = δ * u[1] * u[2] - γ * u[2]\n    return nothing\nend\n\nlv_true(u) = [1.5 * u[1] - u[1] * u[2], u[1] * u[2] - 3.0 * u[2]]\n\nu0 = [1.0, 1.0]\ntspan = (0.0, 3.0)\nts = collect(range(tspan...; length = 15))\n\nClean training trajectory.\n\ntarget = Array(solve(ODEProblem(lv!, u0, tspan), Tsit5(); saveat = ts))","title":"True system"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#Add-observation-noise-(σ*obs*true-0.03)","page":"GP-UDE: Lotka-Volterra","text":"σ_obs_true = 0.03f0\nrng_noise = MersenneTwister(123)\nXnoisy = target .+ σ_obs_true .* randn(rng_noise, size(target))","title":"Add observation noise (σobstrue ≈ 0.03)"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#Build-and-train-the-GP-UDE-field-on-NOISY-data","page":"GP-UDE: Lotka-Volterra","text":"Random.seed!(42)\nZ = kmeans_anchors(Xnoisy, 12; rng = MersenneTwister(7))\n\nlogℓ0=nothing ⇒ data-driven median-heuristic lengthscale init (Gretton et al.), starting the optimizer on the data scale instead of the default ℓ=1, which on some BLAS builds collapses into the wiggly small-ℓ overfit basin. NB: GP-UDE training is multi-basin and BLAS-sensitive (see CLAUDE.md), so the anti-rot gates below are robust API/structural invariants, not exact RMSE.\n\nfield = ExactGPField(SqExponentialKernel(), Z; d = 2, logℓ0 = nothing)\n\nλ=1/(15*2) is a weak log-ℓ prior centred at 0 with std 0.5. We train on Xnoisy — the field must learn through noise, not a clean signal.\n\ntrain!(field, (ts, Xnoisy); tspan, maxiters = 150, λ = 1 / (15 * 2), s = 0.5)","title":"Build and train the GP-UDE field on NOISY data"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#Posterior-GPs","page":"GP-UDE: Lotka-Volterra","text":"g = posterior(field)   # ONE multi-output GP (d=2); mean(g, [u]) → length-2 field","title":"Posterior GPs"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#1.-Trajectory-RMSE-—-ODE-integration-of-the-GP-mean-field-vs-clean-truth","page":"GP-UDE: Lotka-Volterra","text":"PULL propagation uses a first-order Euler step for the mean, which accumulates large errors on a nonlinear limit cycle.  For the mean-trajectory metric we integrate the GP posterior mean field as a proper ODE (same as what training minimises), then compare against the clean truth.\n\ngp_rhs!(du, u, p, t) = (du .= vec(mean(g, [u])); nothing)\nsol_gp = Array(solve(ODEProblem(gp_rhs!, u0, tspan), Tsit5(); saveat = ts))\n\ntraj_pred = [sol_gp[:, i] for i in 1:length(ts)]\ntraj_truth = [target[:, i] for i in 1:length(ts)]\n\nOff-manifold test grid: 10×10 around the limit cycle.\n\nprey_grid = range(0.3, 2.5; length = 10)\npred_grid = range(0.3, 3.5; length = 10)\noffpts = vec([[p1, p2] for p1 in prey_grid, p2 in pred_grid])\n\nmetrics = recovery_metrics(g, lv_true, traj_pred, traj_truth; offpts = offpts)\n\n@info \"Trajectory RMSE (ODE integration of GP mean vs clean truth)\" metrics.traj_rmse\n@info \"Field error (on-trajectory)\"  metrics.field_err_visited.median  metrics.field_err_visited.q90\n@info \"Field error (off-manifold)\"   metrics.field_err_offmanifold.median  metrics.field_err_offmanifold.q90","title":"1. Trajectory RMSE — ODE integration of the GP mean field vs clean truth"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#2.-Plot:-mean-trajectory-vs-clean-data","page":"GP-UDE: Lotka-Volterra","text":"p1 = plot(ts, target[1, :], label = \"prey (clean)\", lw = 2, c = :blue)\nplot!(p1, ts, target[2, :], label = \"pred (clean)\", lw = 2, c = :red)\nplot!(p1, ts, Xnoisy[1, :], label = \"prey (noisy data)\", lw = 1, c = :blue, ls = :dot, alpha = 0.6)\nplot!(p1, ts, Xnoisy[2, :], label = \"pred (noisy data)\", lw = 1, c = :red, ls = :dot, alpha = 0.6)\nplot!(p1, ts, sol_gp[1, :], label = \"prey (GP ODE mean)\", lw = 2, c = :blue, ls = :dash)\nplot!(p1, ts, sol_gp[2, :], label = \"pred (GP ODE mean)\", lw = 2, c = :red, ls = :dash)\nxlabel!(p1, \"t\"); ylabel!(p1, \"population\")\ntitle!(p1, \"LV GP-UDE: mean trajectory (trained on noisy data)\")\nsavefig(p1, \"lv_trajectory.png\")","title":"2. Plot: mean trajectory vs clean data"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#3.-Held-out-initial-condition-—-uncertainty-propagation-coverage","page":"GP-UDE: Lotka-Volterra","text":"We propagate uncertainty from a fresh initial condition two ways and compare coverage of the clean held-out trajectory: PULL (cheap analytic, Euler-limited) and Pathwise (Monte-Carlo ensemble of decoupled GP samples).\n\nu0_test = [1.2, 0.8]   # NOT the training IC\nts_test = collect(range(tspan...; length = 15))\n\nClean ground-truth trajectory from the held-out IC.\n\ntarget_test = Array(solve(ODEProblem(lv!, u0_test, tspan), Tsit5(); saveat = ts_test))\ntruth_vecs = [target_test[:, i] for i in 1:length(ts_test)]","title":"3. Held-out initial condition — uncertainty propagation + coverage"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#3a.-PULL-—-cheap-analytic-propagation-(Euler-limited-on-nonlinear-horizons)","page":"GP-UDE: Lotka-Volterra","text":"PULL's mean is a first-order Euler recurrence; on the LV limit cycle it drifts from the true trajectory, so coverage collapses to ~0. Kept as a documented contrast, NOT as the validated-uncertainty story.\n\nμs_test, Σs_test = propagate(g, u0_test, tspan; method = PULL(), ts = ts_test)\ncov90_pull = coverage(truth_vecs, μs_test, Σs_test; level = 0.9)\n\n@info \"Held-out-IC PULL coverage at 90% nominal (Euler-limited)\" cov90_pull","title":"3a. PULL — cheap analytic propagation (Euler-limited on nonlinear horizons)"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#3b.-Pathwise-—-Monte-Carlo-ensemble-of-decoupled-GP-samples","page":"GP-UDE: Lotka-Volterra","text":"Each of n samples is a decoupled GP draw integrated as a proper ODE, so the ensemble carries the field's uncertainty without PULL's Euler drift. propagate(...; method=Pathwise(n=N)) returns an N × d × |ts| array.\n\nens = propagate(g, u0_test, tspan; method = Pathwise(n = 128), ts = ts_test)\n\nPer-step empirical mean + covariance from the ensemble, then reuse the same Mahalanobis-χ² coverage as PULL (apples-to-apples at 90% nominal).\n\nnsteps = length(ts_test)\nμs_path, Σs_path = pathwise_moments(ens)\ncov90_path = coverage(truth_vecs, μs_path, Σs_path; level = 0.9)\n\n@info \"Held-out-IC Pathwise coverage at 90% nominal (ensemble; nominal-or-conservative)\" cov90_path","title":"3b. Pathwise — Monte-Carlo ensemble of decoupled GP samples"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#4.-Plot:-Pathwise-ensemble-band-over-the-clean-held-out-trajectory","page":"GP-UDE: Lotka-Volterra","text":"5th–95th percentile envelope (90% band) per dimension across the n samples, overlaid on the clean held-out truth. This is the band whose coverage is reported above as cov90_path.\n\nprey_lo = [quantile(ens[:, 1, k], 0.05) for k in 1:nsteps]\nprey_hi = [quantile(ens[:, 1, k], 0.95) for k in 1:nsteps]\npred_lo = [quantile(ens[:, 2, k], 0.05) for k in 1:nsteps]\npred_hi = [quantile(ens[:, 2, k], 0.95) for k in 1:nsteps]\nμmat_path = reduce(hcat, μs_path)   # 2 × |ts_test|\n\np2 = plot(ts_test, target_test[1, :], label = \"prey (clean, held-out IC)\", lw = 2, c = :blue)\nplot!(p2, ts_test, target_test[2, :], label = \"pred (clean, held-out IC)\", lw = 2, c = :red)\nplot!(p2, ts_test, μmat_path[1, :], label = \"prey μ (Pathwise)\", lw = 2, c = :blue, ls = :dash)\nplot!(p2, ts_test, μmat_path[2, :], label = \"pred μ (Pathwise)\", lw = 2, c = :red, ls = :dash)\n\n5–95% Pathwise envelopes.\n\nplot!(p2, ts_test, prey_hi, fillrange = prey_lo, alpha = 0.15, c = :blue, label = \"prey 5–95%\", lw = 0)\nplot!(p2, ts_test, pred_hi, fillrange = pred_lo, alpha = 0.15, c = :red, label = \"pred 5–95%\", lw = 0)\nxlabel!(p2, \"t\"); ylabel!(p2, \"population\")\ntitle!(p2, \"LV GP-UDE: held-out-IC Pathwise ensemble band (90%)\")\nsavefig(p2, \"lv_trajectory_coverage.png\")","title":"4. Plot: Pathwise ensemble band over the clean held-out trajectory"},{"category":"section","location":"generated/gp_ude_lotka_volterra/#Anti-rot-assertions-(#src-lines-run-on-direct-execution-only)","page":"GP-UDE: Lotka-Volterra","text":"Hard gate: trajectory RMSE (ODE integration of GP mean vs clean truth) and on-trajectory field error. Coverage is @info'd — PULL over a nonlinear limit-cycle field can be over/under-dispersed, so a failing coverage number is a calibration finding, not a correctness bug.\n\nGP-UDE training is multi-basin and ill-conditioned, so the trained field — and hence exact recovery RMSE / coverage — is sensitive to the BLAS build / hardware (good fit ≈0.16 here, but a different OpenBLAS can land a poorer basin; see CLAUDE.md roadmap). So these anti-rot gates assert API/STRUCTURAL invariants that hold on ANY backend (catch shape/dispatch regressions), not exact\n\n\n\nThis page was generated using Literate.jl.","title":"Anti-rot assertions (#src lines run on direct execution only)"},{"category":"section","location":"generated/levelset_straddle/#Level-set-recovery-with-Straddle","page":"Level-set recovery (Straddle)","text":"This vignette walks through a complete active-learning experiment: recovering the ellipse level set f(x) = (x₁/1.6)² + (x₂/0.8)² − 1 = 0 in two dimensions using the Straddle acquisition function.","title":"Level-set recovery with Straddle"},{"category":"section","location":"generated/levelset_straddle/#Setup","page":"Level-set recovery (Straddle)","text":"ENV[\"GKSwstype\"] = \"100\"  ## GR headless rendering (no display required)\n\nusing Magpie, KernelFunctions, Random\nusing Plots; gr()","title":"Setup"},{"category":"section","location":"generated/levelset_straddle/#What-is-level-set-estimation?","page":"Level-set recovery (Straddle)","text":"Many scientific tasks ask not where is the minimum of f? but where does f change sign? — the boundary of a feasible region, a decision surface, or a phase boundary.  For minimum-finding, mature packages like Surrogates.jl and BayesianOptimization.jl work well. Magpie's Straddle acquisition targets a contour instead — a different question, and a different tool.\n\nThe Straddle criterion (Bryan et al. 2005) is:\n\na(x) = β σ(x) − |μ(x) − h|\n\nwhere h is the target level, μ(x) and σ(x) are the GP posterior mean and standard deviation, and β is a balancing coefficient. High σ pulls the query toward unexplored space; the penalty |μ − h| pulls it toward the boundary.  Together they concentrate observations exactly where the classification decision is most uncertain — near the level set, not at any extremum.","title":"What is level-set estimation?"},{"category":"section","location":"generated/levelset_straddle/#The-function-and-GP-model","page":"Level-set recovery (Straddle)","text":"In screening tasks the feasible region is often an anisotropic contour — wider along one control than another — so we let the GP learn a separate lengthscale per axis (ARD).\n\nRandom.seed!(42)\n\n# a feasibility boundary that is wider in x₁ than x₂ — different physical scales per axis\nf(x) = (x[1] / 1.6)^2 + (x[2] / 0.8)^2 - 1.0\n\nBuild an ActiveLearner with an ExactGP (ARD squared-exponential kernel, one lengthscale per axis, small observation noise) and the Straddle acquisition targeting h = 0.\n\nal = ActiveLearner(\n    ExactGP(with_lengthscale(SqExponentialKernel(), [0.5, 0.5]); noise = 1.0e-4),\n    Straddle(h = 0.0),\n)","title":"The function and GP model"},{"category":"section","location":"generated/levelset_straddle/#Cold-start","page":"Level-set recovery (Straddle)","text":"Seed the GP with 10 random observations spread over the full domain [−2, 2]² so the model has a rough global picture before the acquisition-guided phase begins.\n\nbox = Box([-2.0, -2.0], [2.0, 2.0])\n\nfor x in [4 .* rand(2) .- 2 for _ in 1:10]\n    observe!(al, x, f(x))\nend","title":"Cold start"},{"category":"section","location":"generated/levelset_straddle/#Active-learning-run","page":"Level-set recovery (Straddle)","text":"run! selects 40 further queries, refitting the GP hyperparameters every 10 steps.  The first call to fit! after the cold start sets the kernel parameters; subsequent refits keep them current as new data arrive.\n\nrun!(al, f; budget = 40, over = box, refit_every = 10)","title":"Active-learning run"},{"category":"section","location":"generated/levelset_straddle/#Results","page":"Level-set recovery (Straddle)","text":"g = posterior_gp(al)\n\nThe fitted ARD lengthscales reflect the ellipse geometry: the x₁ axis (semi-axis 1.6) gets a longer lengthscale than the x₂ axis (semi-axis 0.8).\n\n@info \"recovered ARD lengthscales\" ℓ = round.(1 ./ g.prior.kernel.kernel.transform.v; digits = 3)","title":"Results"},{"category":"section","location":"generated/levelset_straddle/#Figure-1-—-posterior-mean-with-true-contour-and-queries","page":"Level-set recovery (Straddle)","text":"Build a 50×50 evaluation grid and compute the GP posterior mean at each point.  Overlay the true level set (contour! at levels=[0.0]) and mark every point the active learner queried.\n\nxs = range(-2, 2; length = 50)\nys = range(-2, 2; length = 50)\n\nZ_mean = [predmean(g, [xi, yj]) for yj in ys, xi in xs]\nZ_true = [f([xi, yj]) for yj in ys, xi in xs]   ## the true signed-distance field\n\npts = queried_points(al)\npx = [p[1] for p in pts]\npy = [p[2] for p in pts]\n\nplt1 = heatmap(\n    xs, ys, Z_mean;\n    title = \"GP posterior mean\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :RdBu, clim = (-2, 2),\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\n# overlay the TRUE level set (zero contour of f), not the posterior's own contour\ncontour!(plt1, xs, ys, Z_true; levels = [0.0], lw = 2, lc = :black, label = \"true level set (h = 0)\")\nscatter!(\n    plt1, px, py;\n    ms = 4,\n    mc = :white,\n    msw = 1,\n    label = \"queries\",\n)","title":"Figure 1 — posterior mean with true contour and queries"},{"category":"section","location":"generated/levelset_straddle/#Figure-2-—-Straddle-acquisition-surface","page":"Level-set recovery (Straddle)","text":"The acquisition a(g, p) reveals which regions the learner found most informative.  High values (warm colours) lie near the level set where the GP is still uncertain.\n\na = al.acq   ## the Straddle functor, already holds h=0.0\n\nZ_acq = [a(g, [xi, yj]) for yj in ys, xi in xs]\n\nplt2 = heatmap(\n    xs, ys, Z_acq;\n    title = \"Straddle acquisition\",\n    xlabel = \"x₁\", ylabel = \"x₂\",\n    c = :viridis,\n    aspect_ratio = :equal, xlims = (-2, 2), ylims = (-2, 2),\n)\n\nThe example's correctness checks run when this file is executed directly (e.g. in CI); they are stripped from this rendered page.\n\n\n\nThis page was generated using Literate.jl.","title":"Figure 2 — Straddle acquisition surface"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#GP-UDE:-FitzHugh-Nagumo-(UDE-decomposition)","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"Known linear dynamics + a GP that learns the unknown cubic nonlinearity.\n\nThe FitzHugh-Nagumo system is:   v̇ = v - v³/3 - w + I_ext   ẇ = (v + a - b·w) / τ\n\nknown_physics(u, t) = [v - w + I, (v + a - b·w)/τ] supplies the known linear part. The GP must learn only the residual [-v³/3, 0] — a much simpler regression target. This is the UDE (Universal Differential Equation) decomposition pattern: domain knowledge shrinks the GP's task from the full nonlinear field to one scalar cubic term.\n\nCompositeField(known, ExactGPField(...)) implements this split: the outer training optimises the inner GP on the residual only.\n\nRecovery is checked three honest ways:\n\nResidual field error — field_error(posterior(cf), residual_truefield, pts). posterior(cf) returns the RESIDUAL GP (one multi-output GP); we compare against [-v³/3, 0] at visited training states. This is the headline check that the old example omitted.\nTrajectory RMSE — integrate the full composite field (known + GP residual) as an ODE and compare against the clean truth.\nHeld-out-IC uncertainty — propagate(cf, u0_test, tspan; method=Pathwise(n=128)) integrates the FULL composite field (known_physics + GP residual sample) for each ensemble member. Coverage against the clean held-out trajectory is asserted if ≥ 0.6, reported honestly otherwise. PULL on residual-only GPs is included as an informational contrast; the hard gates are still metrics 1 and 2.\n\nIdentifiability note: with tspan=(0,10) and u0=[-1.0, 1.0], the FHN trajectory stays in v ∈ (-1.9, -1.0) (one side of the limit cycle). The cubic is monotone over this range, so the GP does learn the cubic direction at visited states, but coverage of the full cubic range requires a trajectory spanning positive v (period ≈ 40 s). Single- shooting over such a long horizon is unstable, so we work with the partial cycle and report residual field error at visited states honestly.\n\nShort horizon (0, 10) for reliable single-shooting recovery.\n\nENV[\"GKSwstype\"] = \"100\"  ## GR headless\nusing Magpie, OrdinaryDiffEq, SciMLSensitivity, KernelFunctions, LinearAlgebra, Random\nusing Statistics\nusing Plots; gr()\nRandom.seed!(3)","title":"GP-UDE: FitzHugh-Nagumo (UDE decomposition)"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#True-system","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"const FHN_a, FHN_b, FHN_τ, FHN_I = 0.7, 0.8, 12.5, 0.5\n\nfunction fhn!(du, u, p, t)\n    v, w = u[1], u[2]\n    du[1] = v - v^3 / 3 - w + FHN_I\n    du[2] = (v + FHN_a - FHN_b * w) / FHN_τ\n    return nothing\nend\n\nu0 = [-1.0, 1.0]; tspan = (0.0, 10.0); ts = collect(range(tspan...; length = 25))\ntarget = Array(solve(ODEProblem(fhn!, u0, tspan), Tsit5(); saveat = ts))","title":"True system"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#Add-observation-noise-(σ_obs-0.025)","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"σ_obs_true = 0.025f0\nrng_noise = MersenneTwister(7)\nXnoisy = target .+ σ_obs_true .* randn(rng_noise, size(target))","title":"Add observation noise (σ_obs ≈ 0.025)"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#UDE-split-—-known-linear-part;-GP-learns-only-[-v/3,-0]","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"fhn_known(u, t) supplies the known linear dynamics. The true residual the GP must learn is residual_true(u) = [-u[1]³/3, 0].\n\nfhn_known(u, t) = [u[1] - u[2] + FHN_I, (u[1] + FHN_a - FHN_b * u[2]) / FHN_τ]\n\nresidual_true(u) = [-u[1]^3 / 3, 0.0]\n\nBuild the composite field: known-physics closure + trainable residual GP.\n\nZ = kmeans_anchors(Xnoisy, 14; rng = MersenneTwister(3))\n\nlogℓ0=nothing ⇒ data-driven median-heuristic lengthscale init (starts the optimizer on the data scale; see CLAUDE.md on GP-UDE multi-basin / BLAS sensitivity).\n\ninner = ExactGPField(SqExponentialKernel(), Z; d = 2, lognoise = log(1.0e-2), logℓ0 = nothing)\ncf = CompositeField(fhn_known, inner)","title":"UDE split — known linear part; GP learns only [-v³/3, 0]"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#Train-on-noisy-data","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"λ=1/(25*2) is a weak log-ℓ prior centred at 0 with std 0.5.\n\ntrain!(cf, (ts, Xnoisy); tspan, maxiters = 150, λ = 1 / (25 * 2))","title":"Train on noisy data"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#1.-Residual-field-error-—-the-headline-check","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"posterior(cf) returns the RESIDUAL GP (the trained part only; known physics is fixed). We compare the posterior residual mean against the true cubic [-v³/3, 0] at visited states.\n\nThe GP IS learning the cubic over the visited v-range (-1.9, -1.0). Median residual error ≈ 0.84 against a true cubic with median magnitude ≈ 1.49 — the GP cuts the residual to ≈ 0.56× the do-nothing (zero-GP) baseline, so it is doing real work, not sitting at zero. Perfect pointwise recovery would require a full limit-cycle trajectory (period ≈ 40 s; single-shooting at that horizon is numerically unstable).\n\nresidual_g = posterior(cf)\n\nvisited_pts = [target[:, i] for i in 1:size(target, 2)]\nresidual_err = field_error(residual_g, residual_true, visited_pts)\n\nZero-GP baseline: the residual error a do-nothing GP (constant 0) would score — i.e. the median magnitude of the true cubic at visited states. The gate below is RELATIVE to this baseline so it cannot be passed by a GP stuck at zero (which would score exactly it).\n\nzero_gp_baseline = median(norm(residual_true(z)) for z in visited_pts)\n\n@info \"Residual field error at visited states\" residual_err.median residual_err.q90\n@info \"Zero-GP baseline (do-nothing residual error; gate is relative to this)\" zero_gp_baseline ratio = residual_err.median / zero_gp_baseline","title":"1. Residual field error — the headline check"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#2.-Trajectory-RMSE-—-integrate-the-full-composite-field-(known-GP)","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"For the trajectory metric, we need the FULL composite field: known(u,t) + GP(u). We reconstruct it by solving the ODE with both parts active via field_rhs(cf, cf.v0).\n\nNOTE: field_rhs is an UNEXPORTED extension internal (the in-loss RHS builder). There is not yet a public composite mean-trajectory integrator — posterior(cf) returns only the residual GP, and propagate(cf; method=Pathwise()) gives ensembles, not the mean path. Reaching into the extension here is the supported reconstruction path pending a public one.\n\next = Base.get_extension(Magpie, :MagpieSciMLExt)\npf_opt, rhs_opt! = ext.field_rhs(cf, cf.v0)\nsol_full = Array(\n    solve(\n        ODEProblem((du, u, p, t) -> rhs_opt!(du, u, p, t), u0, tspan, pf_opt),\n        Tsit5(); saveat = ts\n    )\n)\n\ntraj_rmse = if size(sol_full) == size(target)\n    sqrt(mean(abs2, sol_full .- target))\nelse\n    @warn \"Composite field ODE diverged — returning Inf RMSE\"\n    Inf\nend\n\n@info \"Trajectory RMSE (full composite field ODE vs clean truth)\" traj_rmse","title":"2. Trajectory RMSE — integrate the full composite field (known + GP)"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#3.-Plot-recovered-residual-vs-true-cubic","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"The cubic term [-v³/3, 0] lives in the v-dimension only. We plot the GP-learned residual mean for dimension 1 (-v³/3) over the v-range of the training trajectory.\n\nv_range = range(minimum(target[1, :]) - 0.2, maximum(target[1, :]) + 0.2; length = 80)\n\nEvaluate at a representative w (trajectory mean) — the residual is w-independent.\n\nw_mid = mean(target[2, :])\nv_pts = [[v, w_mid] for v in v_range]\n\nres_true_v1 = [-z[1]^3 / 3  for z in v_pts]    # true cubic\nres_gp_v1 = [mean(residual_g, [z])[1, 1] for z in v_pts]\nres_gp_v2 = [mean(residual_g, [z])[1, 2] for z in v_pts]  # should be ≈0\n\nScatter the true residual at the actual training states.\n\nv_train = target[1, :]\nres_train = [-v^3 / 3 for v in v_train]\n\np_res = plot(\n    collect(v_range), res_true_v1,\n    label = \"true residual: −v³/3\", lw = 2, c = :black, ls = :dash\n)\nplot!(\n    p_res, collect(v_range), res_gp_v1,\n    label = \"GP learned residual (dim 1)\", lw = 2, c = :blue\n)\nplot!(\n    p_res, collect(v_range), res_gp_v2,\n    label = \"GP learned residual (dim 2, should be ≈0)\", lw = 2, c = :red, ls = :dot\n)\nscatter!(\n    p_res, v_train, res_train,\n    label = \"true −v³/3 at training states\", ms = 3, c = :black, alpha = 0.6, markershape = :circle\n)\nxlabel!(p_res, \"v\"); ylabel!(p_res, \"residual value\")\ntitle!(p_res, \"FHN GP-UDE: learned residual vs true cubic −v³/3\")\nsavefig(p_res, \"fhn_residual.png\")","title":"3. Plot recovered residual vs true cubic"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#4.-Plot-trajectory-recovery","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"p_traj = plot(ts, target[1, :], label = \"v (clean truth)\", lw = 2, c = :blue)\nplot!(p_traj, ts, target[2, :], label = \"w (clean truth)\", lw = 2, c = :red)\nplot!(p_traj, ts, Xnoisy[1, :], label = \"v (noisy data)\", lw = 1, c = :blue, ls = :dot, alpha = 0.6)\nplot!(p_traj, ts, Xnoisy[2, :], label = \"w (noisy data)\", lw = 1, c = :red, ls = :dot, alpha = 0.6)\nif size(sol_full) == size(target)\n    plot!(p_traj, ts, sol_full[1, :], label = \"v (GP-UDE mean)\", lw = 2, c = :blue, ls = :dash)\n    plot!(p_traj, ts, sol_full[2, :], label = \"w (GP-UDE mean)\", lw = 2, c = :red, ls = :dash)\nend\nxlabel!(p_traj, \"t\"); ylabel!(p_traj, \"state\")\ntitle!(p_traj, \"FHN GP-UDE: trajectory recovery (known linear + GP cubic residual)\")\nsavefig(p_traj, \"fhn_trajectory.png\")","title":"4. Plot trajectory recovery"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#5.-Held-out-IC-—-composite-field-uncertainty-propagation","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"propagate(cf, u0_test, tspan; method=Pathwise(n=128)) integrates the FULL composite field (knownphysics + GP residual sample) for each ensemble member. This is the correct coverage check — it includes the deterministic known physics in every sample trajectory. PULL is not implemented for CompositeField (it would need the combined Jacobian of known + GPmean; use Pathwise instead).\n\nu0_test = [-0.8, 1.2]   # NOT the training IC\nts_test = collect(range(tspan...; length = 25))\ntarget_test = Array(solve(ODEProblem(fhn!, u0_test, tspan), Tsit5(); saveat = ts_test))\ntruth_vecs = [target_test[:, i] for i in 1:length(ts_test)]\n\nPathwise ensemble on the FULL composite field (known_physics + GP residual)\n\nens_cf = propagate(cf, u0_test, tspan; method = Pathwise(n = 128), ts = ts_test)\nnsteps = length(ts_test)\nμs_path, Σs_path = pathwise_moments(ens_cf)\ncov90_path = coverage(truth_vecs, μs_path, Σs_path; level = 0.9)\n@info \"CompositeField Pathwise coverage at 90% (full field: known_physics + GP residual)\" cov90_path\n\nAlso report residual-only PULL for contrast (does NOT include known_physics)\n\nμs_res, Σs_res = propagate(residual_g, u0_test, tspan; method = PULL(), ts = ts_test)\ncov90_pull_res = coverage(truth_vecs, μs_res, Σs_res; level = 0.9)\n@info \"Residual-GP PULL coverage at 90% (informational: residual-only, no known_physics)\" cov90_pull_res","title":"5. Held-out IC — composite-field uncertainty propagation"},{"category":"section","location":"generated/gp_ude_fitzhugh_nagumo/#Anti-rot-assertions-(#src-lines-run-on-direct-execution-only)","page":"GP-UDE: FitzHugh-Nagumo (UDE decomp)","text":"GP-UDE training is multi-basin and BLAS-sensitive (see CLAUDE.md), so residual recovery and the composite trajectory RMSE depend on which basin the optimizer lands in — they are reported via @info and shown in the rendered docs (built on Julia 1.12), NOT asserted as exact values. The anti-rot gates are API/structural invariants that hold on ANY backend (catch shape/dispatch/finiteness regressions); the residual GP should still beat the do-nothing baseline in a good basin.\n\n\n\nThis page was generated using Literate.jl.","title":"Anti-rot assertions (#src lines run on direct execution only)"},{"category":"section","location":"#Magpie.jl","page":"Home","text":"<img class=\"docs-light-only\" src=\"assets/logo.svg\" alt=\"Magpie.jl logo\" width=\"240\"/>\n<img class=\"docs-dark-only\" src=\"assets/logo-dark.svg\" alt=\"Magpie.jl logo\" width=\"240\"/>\n\nComposable Gaussian processes for active learning and dynamics — a GP as a differentiable, uncertainty-aware component.\n\nStatus: v0.1, experimental. The incremental-GP spine and active-learning loop are implemented and tested; the GP-in-SciML bridge is the next milestone. APIs may move.","title":"Magpie.jl"},{"category":"section","location":"#What-it-is","page":"Home","text":"Magpie is a Julia-ecosystem-native Gaussian process package where the bridges are the product. It builds on AbstractGPs.jl and KernelFunctions.jl rather than reinventing the GP core, and treats a GP as a differentiable, uncertainty-aware building block that composes with the rest of Julia — autodiff (Mooncake), SciML, and ML.\n\nTwo capabilities hang off one shared, incrementally-updated GP spine:\n\nActive learning — a composable fit → acquire → observe → update loop with level-set / boundary acquisitions (Straddle, BALD-for-classification) that are not yet available in Julia's GP / Bayesian-optimization packages.\nGP-in-SciML (next milestone) — a GP as the right-hand side of an ODE, trained through the solver, for data-efficient, uncertainty-calibrated dynamics discovery.","title":"What it is"},{"category":"section","location":"#Ecosystem-context","page":"Home","text":"Magpie builds on AbstractGPs.jl and KernelFunctions.jl — it reuses their GP core and kernel library rather than reinventing them.\n\nFor optimization-style infill (expected improvement, upper confidence bound, SRBF), Surrogates.jl and BayesianOptimization.jl are mature choices; they target minima. Magpie's active-learning acquisitions (Straddle, BinaryBALD) target level sets and classification boundaries instead — a different objective these packages don't aim at.\n\nFor GP-as-ODE-field work, GPDiffEq.jl is the original proof of concept and deserves the credit; Magpie's Capability B (not yet built) continues that direction on a through-solver, Mooncake-trained path.","title":"Ecosystem context"},{"category":"section","location":"#Examples","page":"Home","text":"See the Examples section in the navigation sidebar for worked vignettes.","title":"Examples"},{"category":"section","location":"#Install","page":"Home","text":"Not yet registered — add from GitHub:\n\npkg> add https://github.com/jowch/Magpie.jl","title":"Install"}]
}
